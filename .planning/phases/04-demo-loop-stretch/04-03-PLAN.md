---
phase: 04-demo-loop-stretch
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - backend/graph.py
  - backend/main.py
  - frontend/lib/api.ts
  - frontend/components/MemoryGraphView.tsx
  - frontend/app/page.tsx
  - frontend/package.json
autonomous: false
requirements: [GRAPH-01, STRETCH-04]
user_setup: []
must_haves:
  truths:
    - "GET /graph returns real Cognee-extracted {nodes, links} aggregated across the live datasets, with node text bodies trimmed to labels"
    - "The main page has a search/graph tab toggle; the graph tab renders a 3D force-directed graph of incidents/fixes/components"
    - "Clicking a node surfaces its detail (STRETCH-04 click-to-explore)"
  artifacts:
    - "backend/graph.py (GET /graph)"
    - "frontend/components/MemoryGraphView.tsx (client-only ForceGraph3D)"
    - "getMemoryGraph() wrapper in frontend/lib/api.ts"
    - "react-force-graph in frontend/package.json"
  key_links:
    - "backend/graph.py reuses _active_search_datasets() so /graph and /search never disagree on live datasets"
    - "aggregate_multi_user_graphs combines per-dataset-isolated graphs into one dense view"
    - "MemoryGraphView uses dynamic(() => import('react-force-graph'), {ssr:false}) inside a 'use client' file (Next 16 requirement)"
---

<objective>
Ship the real Cognee memory graph (GRAPH-01) as a 3D force-directed view behind a tab toggle on the main page, with click-to-explore nodes (STRETCH-04 folds in per D-07). Backend exports the actual `cognify`-produced graph via `get_graph_data()` + `aggregate_multi_user_graphs`; frontend renders it with `ForceGraph3D`. This honestly proves "Cognee builds a real knowledge graph, not just a search index" — the heavily-weighted Best Use of Cognee axis.

Purpose: GRAPH-01 / D-06 / D-07 / D-08 + STRETCH-04 (D-10 notes STRETCH-04 lands with the graph).
Output: GET /graph endpoint + MemoryGraphView + search/graph tab toggle on page.tsx.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full loop under 120 seconds, reset the demo in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

**Slice outcome after this plan:** a real user can toggle to a Graph tab and see + click through the actual Cognee knowledge graph of incidents, fixes, and components.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-CONTEXT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — react-force-graph (+ transitive three [SUS])</name>
  <action>BLOCKING HUMAN CHECKPOINT (not auto-executable, never auto-approvable). Do NOT run `npm install react-force-graph` (Task 3) until a human verifies package legitimacy per the steps below and replies "approved". RESEARCH.md rates react-force-graph OK but flags its transitive `three` dependency [SUS] (too-new heuristic false positive). This gate exists because installing an unverified [SUS]-flagged dependency chain is a supply-chain tampering risk (T-04-SC).</action>
  <what-built>Nothing yet — this gate precedes the `npm install react-force-graph` in Task 3. RESEARCH.md's Package Legitimacy Audit rates react-force-graph OK (33,845 wk downloads, vasturiano/react-force-graph) but flags its transitive dependency `three` as [SUS] — a `too-new` heuristic false positive (three is 10+ years old, 11.5M wk downloads, mrdoob/three.js; only its latest patch is recent). Per protocol, a [SUS] package requires a blocking-human verification before install and is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-force-graph — confirm repo is github.com/vasturiano/react-force-graph, healthy weekly downloads, not a typosquat.
    2. Open https://www.npmjs.com/package/three — confirm repo is github.com/mrdoob/three.js and the 11M+ weekly download count (this is the transitive dep pulled in via 3d-force-graph; the [SUS] flag is only because its most recent patch published recently).
    3. Confirm no other unexpected package is being added (only react-force-graph + its transitive chain).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install react-force-graph`, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: GET /graph endpoint — Cognee graph export + multi-dataset aggregation</name>
  <files>backend/graph.py, backend/main.py</files>
  <read_first>
    - backend/datasets_router.py (GET endpoint shaping a live Cognee list into JSON; config-before-cognee import order lines 10-19; per-item try/except so one bad dataset does not break the whole response lines 62-72)
    - backend/search.py (_active_search_datasets lines 88-117 — reuse for the live dataset list; _all_workaround_dataset_names lines 120-129)
    - backend/main.py (include_router block + import block)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Pattern 3 ("Cognee's own graph-export + multi-dataset aggregation API") + Code Examples "Reshaping cognee's graph tuple format" — the exact call + return shape + the "Trim large fields" note
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "backend/graph.py" (verbatim skeleton + import path note for aggregate_multi_user_graphs)
  </read_first>
  <action>
    Create backend/graph.py as a new APIRouter with forget.py's config-before-cognee import order. Add `@router.get("/graph") async def get_memory_graph()`:
    (1) `names = await _active_search_datasets()` (import from backend.search — reuse the same live-dataset discipline /search and /datasets already share; do not re-derive).
    (2) `user = await get_default_user()` (from cognee.modules.users.methods); `datasets = await get_authorized_existing_datasets(names, "read", user)` (from cognee.modules.data.methods); `pairs = [(user, ds) for ds in datasets]`.
    (3) `nodes, edges = await aggregate_multi_user_graphs(pairs)` — import via its full internal path `from cognee.modules.visualization.cognee_network_visualization import aggregate_multi_user_graphs` (NOT top-level; the wrapper visualize_multi_user_graph uses this same internal path, so it is a sanctioned code path).
    (4) Reshape to `{"nodes": [...], "links": [...]}` using the RESEARCH.md to_force_graph_json shape: each node → `{id, label: (props.name or props.type or id) truncated to 80 chars, group: props.type or "unknown"}`; each edge tuple (source_id, target_id, relationship_name, props) → `{source, target, label: relationship_name}`. CRITICAL (T-04-06 / RESEARCH.md "Trim large fields"): do NOT forward raw DocumentChunk `text`/full-content properties — only id/label/group/relationship survive the reshape.
    On exception: `logger.exception("graph export failed")` + return `{"status":"error","message":"Could not load memory graph. Please try again."}` (D-24). Consider per-dataset defensive wrapping (datasets_router style) so one bad dataset does not empty the whole graph. Register in backend/main.py (import block + app.include_router(graph_router)).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && grep -q 'from cognee.modules.visualization.cognee_network_visualization import aggregate_multi_user_graphs' backend/graph.py && grep -q '_active_search_datasets' backend/graph.py && grep -q 'graph_router' backend/main.py && echo GRAPH_ROUTE_WIRED</automated>
    <human-check>With the enriched corpus (Plan 01) restored and backend running single-worker: `curl http://localhost:8000/graph` returns JSON with non-empty "nodes" and "links" arrays; grep the response for a "text" or "full" chunk-body key and confirm NONE is present (only id/label/group on nodes, source/target/label on links). Node/link counts are visibly higher than a pre-enrichment run (density from Plan 01).</human-check>
  </verify>
  <acceptance_criteria>
    - backend/graph.py exists; GET /graph returns {nodes:[...], links:[...]} shaped exactly as above, aggregated across the live datasets via aggregate_multi_user_graphs — first command prints GRAPH_ROUTE_WIRED.
    - Node objects carry only id/label/group; link objects only source/target/label. No raw chunk text/body field is present in the response (T-04-06).
    - Errors return the fixed human message, never raw exception text (D-24).
    - Registered in main.py.
  </acceptance_criteria>
  <done>Real Cognee graph exported + aggregated + trimmed to a force-graph-ready payload; registered.</done>
</task>

<task type="auto">
  <name>Task 3: 3D graph view + tab toggle + click-to-explore (STRETCH-04) + api wrapper</name>
  <files>frontend/package.json, frontend/lib/api.ts, frontend/components/MemoryGraphView.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/lib/api.ts (listDatasets throw-on-!ok shape lines 314-320 — mirror for getMemoryGraph; DatasetInfo interface style)
    - frontend/components/DatasetList.tsx (useQuery({queryKey, queryFn}) read pattern lines 155-158 — for the graph data fetch)
    - frontend/app/page.tsx (current single-page section layout + useState pattern — where the search/graph toggle state and graph section land)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Code Examples "Client-only ForceGraph3D component" + Pitfall 3 (ssr:false must be inside a "use client" file) + Pitfall 4 (B-04 CSS landmine)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md sections "frontend/components/MemoryGraphView.tsx" + "frontend/lib/api.ts" + "frontend/app/page.tsx" (verbatim skeletons)
    - node_modules/next/dist/docs lazy-loading guide (per frontend/AGENTS.md — Next 16 dynamic import + ssr:false rules)
  </read_first>
  <action>
    (a) After Task 1 approval, run `npm install react-force-graph` in frontend/ (D-07 names the umbrella package explicitly; three arrives transitively). Confirm frontend/package.json now lists react-force-graph.
    (b) Add getMemoryGraph() to frontend/lib/api.ts mirroring listDatasets: interfaces `GraphNode {id,label,group}`, `GraphLink {source,target,label}`, `GraphData {nodes:GraphNode[], links:GraphLink[]}`; GET `${API_BASE}/graph`, throw on !ok ("Could not load memory graph.").
    (c) Create frontend/components/MemoryGraphView.tsx ("use client") using RESEARCH.md's verified pattern: `const ForceGraph3D = dynamic(() => import("react-force-graph").then((mod) => mod.ForceGraph3D), { ssr: false })` (the dynamic/ssr:false call MUST live inside this "use client" file — Pitfall 3). Render `<ForceGraph3D graphData={graphData} nodeLabel="label" nodeAutoColorBy="group" linkLabel="label" onNodeClick={...} />`. Implement onNodeClick to surface the clicked node's detail (STRETCH-04 click-to-explore) — e.g. set local selected-node state and render its label/group/id in a small side panel or toast; keep labels readable on a demo screen (D-07). A page-level container fetches graphData via useQuery({queryKey:["graph"], queryFn:getMemoryGraph}) and feeds it in.
    (d) In frontend/app/page.tsx add a search/graph tab toggle: `const [view, setView] = useState<"search"|"graph">("search")` gating which section renders (D-08 — tab on the main page, NO navigation away). Keep the existing search/upload/datasets sections under the "search" view; render `<MemoryGraphView />` container under the "graph" view. Any new styling uses Tailwind utilities — never introduce a `*/` inside a CSS comment (B-04).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && node -e "const p=require('./package.json'); if(!p.dependencies['react-force-graph']) throw new Error('missing'); console.log('DEP_OK')" && grep -q 'export async function getMemoryGraph' lib/api.ts && grep -q 'ssr: false' components/MemoryGraphView.tsx && grep -q 'onNodeClick' components/MemoryGraphView.tsx && grep -Eq 'view.*graph|graph.*view' app/page.tsx && echo GRAPH_UI_WIRED</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && npx tsc --noEmit && echo TSC_OK</automated>
    <human-check>`npm run dev` and load http://localhost:3000 with the backend running: NO PostCSS error and NO "window is not defined"/hydration error (Pitfall 3/4); a search/graph toggle is visible; switching to Graph renders a 3D force-directed graph with readable node labels; clicking a node shows its detail (STRETCH-04); switching back to Search keeps the search UI intact.</human-check>
  </verify>
  <acceptance_criteria>
    - react-force-graph is in package.json; getMemoryGraph() exists; MemoryGraphView uses ssr:false dynamic import inside a "use client" file and has an onNodeClick handler; page.tsx has the search/graph toggle — first command prints GRAPH_UI_WIRED.
    - `npx tsc --noEmit` passes — second command prints TSC_OK.
    - Live browser: graph tab renders a 3D graph with labels, click-to-explore works, no window/SSR/PostCSS error, search view unaffected.
  </acceptance_criteria>
  <done>3D memory graph renders behind a main-page tab with click-to-explore; browser-verified clean (B-04 + SSR).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /graph | Untrusted request crosses to a server-side graph export (localhost, CORS-locked) |
| backend → Cognee graph engine | Raw (nodes, edges) incl. full chunk text must be trimmed before crossing to the browser |
| npm registry → frontend build | Supply-chain: react-force-graph + transitive three |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm install react-force-graph (transitive three [SUS]) | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying npmjs.com repos before install; never auto-approvable; three [SUS] is a documented too-new heuristic false positive |
| T-04-06 | Information Disclosure | GET /graph payload | medium | mitigate | Trim DocumentChunk text/body fields — only id/label/group/relationship survive the reshape; bounds payload size as corpus grows (D-09) |
| T-04-07 | Access Control (V4/V5) | /graph endpoint | low | accept | Unauthenticated + no user input, consistent with every existing endpoint (localhost-only, CORS-locked); if a dataset filter param is added later, validate it like forget.py's regex + live-list check |
| T-04-08 | DoS / client crash | ForceGraph3D SSR boundary + CSS comment | medium | mitigate | dynamic(ssr:false) inside a "use client" file (Pitfall 3); no `*/` inside CSS comments (B-04) — both caught only by a live browser load, which the human-check performs |
</threat_model>

<verification>
- Task 1 approval recorded; GRAPH_ROUTE_WIRED + GRAPH_UI_WIRED + TSC_OK.
- Live: curl /graph returns trimmed nodes/links (no chunk text); graph tab renders 3D + click-to-explore; no SSR/PostCSS error.
</verification>

<success_criteria>
- GET /graph returns the real, aggregated, text-trimmed Cognee graph.
- The main page toggles to a readable, clickable 3D memory graph proving Cognee's real knowledge graph (Best Use of Cognee axis).
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| backend/graph.py | module + route | GET /graph; get_memory_graph() |
| graph_router registration | main.py edit | app.include_router(graph_router) |
| getMemoryGraph() | api wrapper | frontend/lib/api.ts; GraphNode/GraphLink/GraphData interfaces |
| frontend/components/MemoryGraphView.tsx | component | client-only ForceGraph3D + onNodeClick (STRETCH-04) |
| search/graph tab toggle | page.tsx edit | useState<"search"|"graph"> |
| react-force-graph | dependency | frontend/package.json |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-03-SUMMARY.md` when done.
</output>
