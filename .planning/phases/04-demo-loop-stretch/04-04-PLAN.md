---
phase: 04-demo-loop-stretch
plan: 04
type: execute
wave: 4
depends_on: [04-02, 04-03]
files_modified:
  - scripts/time_demo_loop.py
autonomous: true
requirements: [DEMO-03]
must_haves:
  truths:
    - "A timing harness runs the full search → release upload → drift appears → forget → re-search loop against the live backend and reports the total wall-clock time"
    - "The measured total is under 120 seconds locally (DEMO-03), and each step's re-search survives forget (B-01 confirmed in the timed path)"
    - "GET /graph and POST /reset both respond as part of confirming the full demo works before stretch begins"
  artifacts:
    - "scripts/time_demo_loop.py (loop timing harness + demo script of record)"
  key_links:
    - "The harness POSTs /reset first to guarantee a clean, workarounds_v1_8-present start state (Plan 02)"
    - "This plan is the GATE: Plans 05 and 06 (stretch) depend on it (D-10 — stretch gated behind a confirmed working loop)"
---

<objective>
Verify DEMO-03: the full search → release upload → drift badge → forget → re-search loop finishes under 120 seconds locally, and confirm reset (Plan 02) + graph (Plan 03) work — the gate D-10 requires before any stretch feature is built. The loop itself was built in Phases 2-3 and RESEARCH.md confirmed B-01 is resolved (re-search survives forget); this plan MEASURES and RECORDS it, it does not rebuild it.

Purpose: DEMO-03 / D-01 (loop runs locally, recorded) / D-02 (canonical query + visible forget-proof) / D-10 gate.
Output: scripts/time_demo_loop.py — an automated timing harness that doubles as the demo script of record.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full loop under 120 seconds, reset the demo in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

**Slice outcome after this plan:** the operator has a repeatable, timed proof (and a written script) that the loop lands under 120s — the green light to build stretch depth.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-CONTEXT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loop timing harness + demo script of record (DEMO-03 / D-01 / D-02)</name>
  <files>scripts/time_demo_loop.py</files>
  <read_first>
    - backend/search.py (POST /search request shape SearchRequest {query}; response keys root_cause/source_dataset/drift_state; _active_search_datasets re-queries live datasets each request — the B-01 defense)
    - backend/ingest.py (POST /ingest + POST /ingest/status + POST /sample/load contracts; asyncio.create_task background cognify — the release-upload step)
    - backend/datasets_router.py (GET /datasets returns per-dataset drift_state/drift_reason — the "drift badge appears" step)
    - backend/forget.py (POST /forget {dataset} — only a drifting workarounds_v{N} is forgettable)
    - frontend/lib/api.ts (the exact endpoint payloads the browser sends, so the harness matches the real demo flow)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Summary + Pattern 2 (B-01 resolved: re-search survives forget; _active_search_datasets re-queries fresh) + §"Open Questions" 1 (canonical query = "customers double-charged"; visible proof = dataset-row-vanish + drift-badge-flip, NOT evidence-chunk diff — evidence is not interleaved per STATE.md)
    - .planning/STATE.md §"Blockers/Concerns" (Phase-3 evidence-non-interleave note; reseed-bills-cognify)
  </read_first>
  <action>
    Create scripts/time_demo_loop.py — a standalone timing harness that drives the demo loop against the RUNNING backend via HTTP (matching the browser flow in frontend/lib/api.ts), using only the standard library (urllib) or requests if already available; do NOT import cognee (this measures the real network path). Its module docstring is the demo script of record: canonical query "customers double-charged", release upload of the v1.9 workaround, and the visible proof = the workarounds_v1_8 dataset row vanishing + the drift-badge flip (NOT an evidence-chunk diff — evidence is not interleaved for this query, per STATE.md). Steps, each wall-clock timed with time.perf_counter() and printed as "step: N.Ns":
    1. POST /reset — guarantee a clean, workarounds_v1_8-present start (uses Plan 02); wait for {"status":"reset"}. (Not counted in the loop budget — it is demo setup.)
    2. START timer. POST /search {query:"customers double-charged"} — assert status ok and the answer reflects the v1.8/dedup fix.
    3. POST /ingest (or /sample/load) uploading the release note that supersedes v1_8; poll GET /ingest/status until ready (bounded timeout).
    4. GET /datasets — assert workarounds_v1_8 now has drift_state == "drifting" with a drift_reason (drift badge appeared).
    5. POST /forget {dataset:"workarounds_v1_8"} — assert {"status":"forgotten"}.
    6. POST /search {query:"customers double-charged"} again — assert status ok (re-search SURVIVES forget — the B-01 confirmation) and the answer now reflects the v1.9 idempotency_guard fix; assert workarounds_v1_8 is absent from GET /datasets. STOP timer.
    Print per-step timings + TOTAL and assert TOTAL < 120.0 (exit non-zero if exceeded). ALSO probe GET /graph (assert non-empty nodes) so the gate confirms the graph works too. If any step returns {"status":"error"}, fail loudly with the step name (per RESEARCH Pitfall 1 warning-signs, a real failure here is more likely a Mistral hiccup or an in-flight race than the disproven shared-collection theory). The harness restores clean state by POSTing /reset again at the end.
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && .venv/Scripts/python.exe -c "import ast; ast.parse(open('scripts/time_demo_loop.py').read()); src=open('scripts/time_demo_loop.py').read(); assert 'import cognee' not in src, 'harness must not import cognee'; assert '/reset' in src and '/forget' in src and '/graph' in src and '120' in src; assert 'customers double-charged' in src; print('HARNESS_SHAPE_OK')"</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && .venv/Scripts/python.exe scripts/time_demo_loop.py</automated>
    <human-check>Record the on-screen browser loop for the D-01 submission video: from the first search keystroke to seeing the updated v1.9 answer is under 120 seconds; the workarounds_v1_8 row visibly vanishes and its 🔴 badge flips; the Reset button (Plan 02) and Graph tab (Plan 03) both work on camera. Confirm the harness's printed TOTAL is under 120s.</human-check>
  </verify>
  <acceptance_criteria>
    - scripts/time_demo_loop.py exists, imports no cognee, drives /reset → search → ingest release → /datasets drift → /forget → re-search → /graph, and asserts TOTAL < 120s — first command prints HARNESS_SHAPE_OK.
    - Running the harness against the live backend exits 0 with printed per-step timings and a TOTAL under 120.0s (DEMO-03); the post-forget re-search returns status ok (B-01 confirmed in the timed path) and reflects the v1.9 fix; workarounds_v1_8 is absent afterward.
    - GET /graph returns non-empty nodes during the run (graph confirmed).
    - Human: the recorded on-screen loop is under 120s with the visible dataset-row-vanish + drift-flip proof; reset + graph work on camera.
  </acceptance_criteria>
  <done>Automated + recorded proof that the full loop lands under 120s locally, reset + graph confirmed — the gate for stretch is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timing harness → backend endpoints | Local script drives real endpoints (localhost); no new server surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Denial of Service / data corruption | harness mutates live memory (forget) | low | mitigate | Harness POSTs /reset at start AND end to guarantee a clean workarounds_v1_8-present state before and after; it never leaves memory in a forgotten state |
| T-04-10 | Tampering | harness is a verification script only | low | accept | No new endpoint or input surface; the harness only exercises endpoints already built and threat-modeled in Plans 02/03 and Phases 2-3 |
</threat_model>

<verification>
- HARNESS_SHAPE_OK (static); harness run exits 0 with TOTAL < 120s and all step assertions passing.
- Human: recorded loop under 120s with the visible forget-proof; reset + graph confirmed on camera.
</verification>

<success_criteria>
- DEMO-03 proven: the full loop finishes under 120s locally, recorded (D-01), with the canonical query + dataset-row-vanish proof (D-02).
- Reset + graph confirmed working — the D-10 gate for Plans 05/06 is satisfied.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| scripts/time_demo_loop.py | script | loop timing harness (stdlib HTTP, no cognee import) + demo script of record; asserts TOTAL < 120s |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-04-SUMMARY.md` when done.
</output>
