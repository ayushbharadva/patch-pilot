---
phase: 04-demo-loop-stretch
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/reset.py
  - backend/main.py
  - frontend/lib/api.ts
  - frontend/components/ui/dialog.tsx
  - frontend/components/ResetButton.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [DEMO-01]
must_haves:
  truths:
    - "POST /reset restores the fresh snapshot and returns {status: reset} without a Windows PermissionError, and a search works immediately after"
    - "The demo operator can click one Reset button, confirm in a modal, watch a reset animation, and land on a clean demo-ready memory state"
    - "A failed reset shows a short human message, never a raw exception"
  artifacts:
    - "backend/reset.py (POST /reset)"
    - "frontend/components/ResetButton.tsx (modal confirm + animation)"
    - "frontend/components/ui/dialog.tsx (shadcn dialog primitive)"
    - "resetMemory() wrapper in frontend/lib/api.ts"
  key_links:
    - "backend/reset.py releases relational + vector + graph engine handles (Pattern 4) BEFORE scripts.snapshot_memory.restore() — else rmtree fails on Windows"
    - "ResetButton invalidates ALL react-query caches on success so DatasetList + graph re-fetch the restored state"
    - "backend/main.py app.include_router(reset_router) registers the endpoint"
---

<objective>
Ship the one-click demo reset (DEMO-01): a POST /reset endpoint that releases Cognee's open DB file handles and restores the fresh snapshot from Plan 01, plus a guarded modal reset button with a visible animation on the main page. Reset = snapshot restore (D-03), NOT prune+reseed — instant, zero-cost, repeatable, and rebuilds every dataset's files from a known-good state.

Purpose: DEMO-01 / D-03 / D-04 / D-05 — make the demo repeatable on-camera in one click without re-billing cognify.
Output: working POST /reset + ResetButton with modal confirm + reset animation, wired into page.tsx.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full loop under 120 seconds, reset the demo in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

**Slice outcome after this plan:** a real user can click Reset, confirm, and watch memory return to the clean demo-ready state — the loop can be re-run from the top immediately.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-CONTEXT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /reset endpoint — engine-handle release + snapshot restore</name>
  <files>backend/reset.py, backend/main.py</files>
  <read_first>
    - backend/forget.py (router shape, config-before-cognee import order lines 16-24, D-24 error handling lines 88-90, _MSG_ERROR pattern)
    - scripts/snapshot_memory.py (restore() at lines 58-67 — the pure-filesystem restore this endpoint delegates to; do NOT add a cognee import to this module)
    - backend/main.py (include_router block lines 106-110 + the from backend.X import router as X_router block lines 43-48)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Pattern 4 ("Windows-safe file-handle release") + Code Examples "Reset endpoint skeleton" — the load-bearing engine-release sequence
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "backend/reset.py" (verbatim skeleton + import order)
  </read_first>
  <action>
    Create backend/reset.py as a new APIRouter following forget.py's config-before-cognee import order verbatim (backend.cognee_config → cognee → fastapi → backend.cognee_patches). Define `_MSG_ERROR = "Could not reset memory. Please try again."` (D-24). Add `@router.post("/reset") async def reset_memory()` implementing RESEARCH.md Pattern 4 EXACTLY in this order inside a try block:
    (1) `db_engine = get_relational_engine()` then `await db_engine.engine.dispose()` — releases the SQLite/-shm/-wal handles that plain lru_cache does NOT close (the gap prune_system omits; on Windows this is mandatory or shutil.rmtree raises PermissionError WinError 32).
    (2) `create_relational_engine.cache_clear()` (plain lru_cache — drops the stale reference).
    (3) `_create_vector_engine.cache_clear()` and `_create_graph_engine.cache_clear()` (closing_lru_cache — each eviction invokes the adapter's real close(), releasing LanceDB/Kuzu native handles).
    (4) `from scripts import snapshot_memory; snapshot_memory.restore()` — delegate the filesystem swap to the unmodified script.
    Return `{"status": "reset"}` on success. On any exception: `logger.exception("reset failed")` + return `{"status": "error", "message": _MSG_ERROR}` (never leak raw exception text — D-24). Import the private engine factories inside the function body (as the skeleton shows) so import cost is paid only on reset. Then register in backend/main.py: add `from backend.reset import router as reset_router` in the alphabetized import block and `app.include_router(reset_router)` in the trailing block. No CORS change (same origin allowlist already covers same-origin POST).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && .venv/Scripts/python.exe -c "import ast; t=ast.parse(open('backend/reset.py').read()); src=open('backend/reset.py').read(); assert 'engine.dispose()' in src and '_create_vector_engine.cache_clear()' in src and '_create_graph_engine.cache_clear()' in src and 'snapshot_memory.restore()' in src; import re; assert src.index('engine.dispose()') < src.index('snapshot_memory.restore()'), 'dispose must precede restore'; print('RESET_ORDER_OK')"</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && grep -q 'from scripts import snapshot_memory' scripts/snapshot_memory.py; test $? -ne 0 && grep -c 'import cognee' scripts/snapshot_memory.py | grep -q '^0$' && echo SNAPSHOT_STAYS_PURE || echo SNAPSHOT_STAYS_PURE</automated>
    <human-check>With the enriched snapshot present (Plan 01) and backend running single-worker (`.venv/Scripts/python.exe -m uvicorn backend.main:app --workers 1 --host 127.0.0.1 --port 8000` from repo root): forget a drifting dataset via the UI, then `curl -X POST http://localhost:8000/reset` returns {"status":"reset"} with NO PermissionError in server logs; a subsequent POST /search for "customers double-charged" returns status ok (memory restored, forgotten dataset back).</human-check>
  </verify>
  <acceptance_criteria>
    - backend/reset.py exists; POST /reset returns {"status":"reset"} on success and {"status":"error","message": "Could not reset memory. Please try again."} on failure (no raw exception text).
    - The engine-release sequence runs BEFORE snapshot_memory.restore() (dispose → cache_clear x3 → restore) — first command prints RESET_ORDER_OK.
    - scripts/snapshot_memory.py still imports NO cognee (stays pure filesystem) — second command prints SNAPSHOT_STAYS_PURE.
    - Live: reset after a forget restores memory with no Windows PermissionError; a post-reset search succeeds.
  </acceptance_criteria>
  <done>POST /reset restores the fresh snapshot with correct Windows-safe handle release; registered in main.py; snapshot script unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reset button + modal confirm + animation + api wrapper</name>
  <files>frontend/components/ui/dialog.tsx, frontend/lib/api.ts, frontend/components/ResetButton.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/components/DatasetList.tsx (ForgetButton lines 39-111 — mutation/isX/error local-state + toast-on-success shape to REUSE; note Phase 3 used inline two-step, this uses a modal instead per D-05)
    - frontend/lib/api.ts (forgetDataset lines 272-298 — the discriminated-union + try/catch-to-error-variant wrapper shape to mirror for resetMemory)
    - frontend/app/page.tsx (section-per-feature layout lines 79-85; QueryClient usage — where the reset button lands)
    - frontend/components/ui/button.tsx (Button variants: destructive, ghost)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md sections "frontend/components/ResetButton.tsx" + "frontend/lib/api.ts" (verbatim wrapper + component skeletons)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Supporting Libraries (shadcn dialog install) + Pitfall 4 (the */-in-CSS-comment landmine, B-04)
    - node_modules/next/dist/docs (per frontend/AGENTS.md: this is Next 16 — read the relevant guide before writing app-router code)
  </read_first>
  <action>
    (a) Generate the dialog primitive: run `npx shadcn add dialog` in frontend/ (creates frontend/components/ui/dialog.tsx from the already-installed radix-ui@^1.6.1 — no new external package, so no legitimacy checkpoint needed). This is the first modal primitive in the codebase (Phase 3 used inline confirm).
    (b) Add resetMemory() to frontend/lib/api.ts mirroring forgetDataset's shape: a discriminated union `ResetResponse = {status:"reset"} | {status:"error", message:string}`, POST to `${API_BASE}/reset`, normalize network/parse failures into the error variant with the fixed message "Could not reset memory. Please try again." (MUST match backend/reset.py _MSG_ERROR verbatim, same discipline as forgetDataset's fallback).
    (c) Create frontend/components/ResetButton.tsx ("use client"): reuse ForgetButton's local-state shape (open/isResetting/error) but use the new Dialog/DialogTrigger/DialogContent/DialogFooter primitives (D-05 modal) instead of the inline two-step. Trigger = a destructive Button "Reset Demo"; modal asks "Reset memory to demo-ready state?" with Cancel + Confirm Reset. On confirm: call resetMemory(); while pending show a VISIBLE reset animation (D-04 — an intentional progress/pulsing indicator, not a bare spinner; the button reads "Resetting…" and is disabled, satisfying the DoS mitigation T-04-03); on success setOpen(false) + `await queryClient.invalidateQueries()` (invalidate ALL caches so DatasetList and the future graph re-fetch) + toast.success("Memory reset to demo-ready state"); on error show result.message inline (destructive text).
    (d) Place `<ResetButton />` in frontend/app/page.tsx near the header/top as a new element (same aria-label convention as existing sections). Any styling uses Tailwind utilities — do NOT introduce a `*/` sequence inside any CSS comment (B-04 crashes the whole frontend; only a live browser load catches it).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && test -f components/ui/dialog.tsx && grep -q 'export async function resetMemory' lib/api.ts && grep -q 'invalidateQueries' components/ResetButton.tsx && grep -q 'ResetButton' app/page.tsx && echo RESET_UI_WIRED</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && npx tsc --noEmit && echo TSC_OK</automated>
    <human-check>Run `npm run dev` in frontend/ and load http://localhost:3000: the page loads with NO PostCSS/build error (B-04 clear); a "Reset Demo" button is visible; clicking it opens a modal; Cancel dismisses; Confirm shows a reset animation, then a success toast, and DatasetList refreshes to the clean restored state. Confirm the reset button is disabled while the request is in flight (T-04-03).</human-check>
  </verify>
  <acceptance_criteria>
    - frontend/components/ui/dialog.tsx exists (shadcn-generated); resetMemory() exists in api.ts with the matching fixed error message; ResetButton invalidates queries and is placed in page.tsx — first command prints RESET_UI_WIRED.
    - `npx tsc --noEmit` passes — second command prints TSC_OK.
    - Live browser: page loads with no PostCSS error (B-04); modal confirm + reset animation + success toast + DatasetList refresh all work; button disabled while in flight.
  </acceptance_criteria>
  <done>One-click guarded reset with modal + animation wired end-to-end; browser-verified clean (B-04), invalidates caches on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /reset | Untrusted trigger crosses to a destructive server action (localhost, CORS-locked to http://localhost:3000) |
| backend → filesystem (rmtree + untar) | Open DB handles vs a live filesystem swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | POST /reset spam | low | mitigate | ResetButton disables the trigger + shows the reset animation while a request is in flight (D-04 side effect) — single-operator local demo needs no server-side debounce |
| T-04-04 | Tampering / DoS | shutil.rmtree on live memory tree | high | mitigate | Pattern 4 engine-release sequence (relational dispose + vector/graph cache_clear) BEFORE restore — prevents Windows PermissionError corrupting the memory tree mid-demo |
| T-04-05 | Information Disclosure | /reset error path | low | mitigate | D-24: try/except Exception, logger.exception server-side, fixed _MSG_ERROR to client, never raw exception text |
</threat_model>

<verification>
- RESET_ORDER_OK + SNAPSHOT_STAYS_PURE (backend); RESET_UI_WIRED + TSC_OK (frontend).
- Live: forget → reset → search round-trip works with no PermissionError; modal + animation + toast + cache refresh behave; page loads with no PostCSS error.
</verification>

<success_criteria>
- POST /reset restores the Plan-01 snapshot Windows-safely and idempotently.
- One-click modal-guarded reset with a visible animation returns the demo to clean state on-camera; failures show only a short human message.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| backend/reset.py | module + route | POST /reset; _MSG_ERROR; reset_memory() |
| reset_router registration | main.py edit | app.include_router(reset_router) |
| resetMemory() | api wrapper | frontend/lib/api.ts; ResetResponse union |
| frontend/components/ui/dialog.tsx | ui primitive | shadcn/Radix Dialog (generated) |
| frontend/components/ResetButton.tsx | component | modal confirm + reset animation + invalidateQueries |
| ResetButton placement | page.tsx edit | new top-level element |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-02-SUMMARY.md` when done.
</output>
