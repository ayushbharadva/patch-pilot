---
phase: 04-demo-loop-stretch
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - backend/search.py
  - backend/tests/test_search_helpers.py
  - frontend/lib/api.ts
  - frontend/components/DiagnosisCard.tsx
autonomous: true
requirements: [STRETCH-01]
must_haves:
  truths:
    - "The /search response carries a numeric confidence derived from the CHUNKS retriever's real similarity score (verbose=True), normalized so higher = more confident"
    - "The diagnosis card displays the confidence as a percentage badge beside the version tag"
    - "A search with no grounding still returns cleanly (confidence null) and the card omits the badge"
  artifacts:
    - "verbose=True CHUNKS call + confidence extraction in backend/search.py"
    - "confidence field in SearchResponseOk (frontend/lib/api.ts)"
    - "confidence badge in frontend/components/DiagnosisCard.tsx"
    - "unit tests for the objects_result score path in backend/tests/test_search_helpers.py"
  key_links:
    - "backend/search.py CHUNKS call switches to verbose=True; _flatten_and_truncate reads objects_result (with search_result backward-compat) so existing tests + evidence output stay green"
    - "confidence attaches as a flat top-level /search key like drift_state, mapped 1:1 to the SearchResponseOk interface"
---

<objective>
Ship STRETCH-01: a confidence score on the diagnosis card, derived from the CHUNKS retriever's real similarity score. Per RESEARCH.md Pitfall 5 this is NOT already in the /search payload — cognee's ChunksRetriever discards ScoredResult.score unless `verbose=True` is passed, which reshapes the per-dataset result to expose `objects_result`. This is a scoped, tested backend change plus a small UI badge.

Purpose: STRETCH-01 (D-10 — cheapest stretch win, gated behind the confirmed loop from Plan 04).
Output: real confidence number on /search + a percentage badge on DiagnosisCard.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full loop under 120 seconds, reset the demo in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

**Slice outcome after this plan:** a real user sees a confidence percentage on each diagnosis, reading how strongly the evidence supports the recommendation.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend confidence extraction — verbose CHUNKS + score normalization</name>
  <files>backend/search.py, backend/tests/test_search_helpers.py</files>
  <read_first>
    - backend/search.py (the CHUNKS cognee.search call lines 227-234; _flatten_and_truncate lines 178-197 reading result.get("search_result"); the flat response dict lines 272-282 where drift_state is a top-level key; _pick_primary_result — UNAFFECTED, it reads the separate non-verbose GRAPH_COMPLETION results)
    - backend/tests/test_search_helpers.py (existing _flatten_and_truncate tests pass dicts keyed "search_result" — these must stay green; add new tests for the objects_result path)
    - .planning/phases/04-demo-loop-stretch/04-RESEARCH.md Pitfall 5 (verbose=True reshapes to objects_result/text_result/context_result; objects_result items are ScoredResult-shaped with .payload/.score) + Architectural Responsibility Map row "Confidence score"
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "backend/search.py (modified — STRETCH-01)"
    - Installed cognee source: .venv/Lib/site-packages/cognee/modules/retrieval/chunks_retriever.py and .../modules/search/methods/get_retriever_output.py — CONFIRM the exact objects_result item shape (attribute vs dict access for .payload/.score, whether payload holds the chunk text, and whether score is a distance or a similarity) BEFORE finalizing the parse and the normalization direction.
  </read_first>
  <behavior>
    - Given a verbose per-dataset result with objects_result items carrying scores, _flatten_and_truncate returns the same {excerpt, full_text, source} evidence shape as today (text sourced from each item's payload text field).
    - Given the legacy shape (dict keyed "search_result"), _flatten_and_truncate still returns the identical output (backward compat — existing tests stay green).
    - A new helper extracts a top-of-list confidence in [0,1] from the verbose objects_result scores, normalized so higher = more confident (invert if cognee returns a cosine distance; pass through if it returns a similarity — per the source confirmed in read_first).
    - When no objects_result / no scores are present, confidence is None (never raises).
  </behavior>
  <action>
    Switch the CHUNKS cognee.search() call (lines 227-234) to pass `verbose=True`. Because verbose reshapes each per-dataset result from a flat "search_result" list into `{"objects_result":[...], "text_result":..., "context_result":...}`, update evidence extraction: teach _flatten_and_truncate to read `objects_result` when present (extracting each item's payload text) while FALLING BACK to the current `search_result` key when absent — this keeps existing callers/tests working and the evidence panel output byte-identical. Add a small pure helper `_confidence_from_results(results)` that reads the top objects_result item's score, normalizes to a 0-1 confidence (higher = more confident; invert a distance if that is what cognee returns — confirm via the source read in read_first), and returns None when unavailable. Attach the result as a flat top-level `"confidence"` key on the /search "ok" response, exactly like `drift_state` is attached (lines 272-282). confidence extraction must be best-effort: wrap it so a parse issue yields confidence=None and never fails the search (mirror the qa_id best-effort pattern lines 264-270). Do NOT change _pick_primary_result or the GRAPH_COMPLETION call. Add unit tests in test_search_helpers.py: (i) _flatten_and_truncate over the new objects_result shape returns the correct evidence; (ii) the legacy search_result tests still pass; (iii) _confidence_from_results maps a known top score to the expected normalized confidence and returns None for empty input.
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && .venv/Scripts/python.exe -m pytest backend/tests/test_search_helpers.py -q</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && grep -q 'verbose=True' backend/search.py && grep -q '"confidence"' backend/search.py && grep -q '_confidence_from_results' backend/tests/test_search_helpers.py && echo CONFIDENCE_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - CHUNKS search uses verbose=True; /search "ok" response has a flat top-level "confidence" key (number in [0,1] or null) — second command prints CONFIDENCE_WIRED.
    - _flatten_and_truncate handles BOTH the verbose objects_result shape and the legacy search_result shape; all existing test_search_helpers.py tests remain green and new objects_result + _confidence_from_results tests pass — pytest exits 0.
    - Confidence extraction is best-effort: an unparseable/absent score yields confidence=None and never fails /search.
    - _pick_primary_result and the GRAPH_COMPLETION call are unchanged.
  </acceptance_criteria>
  <done>Real confidence surfaced from verbose CHUNKS scores, normalized higher=better, best-effort, fully unit-tested; evidence output unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Confidence badge on the diagnosis card</name>
  <files>frontend/lib/api.ts, frontend/components/DiagnosisCard.tsx</files>
  <read_first>
    - frontend/lib/api.ts (SearchResponseOk interface lines 21-31 — add confidence 1:1 like drift_state was added)
    - frontend/components/DiagnosisCard.tsx (VersionTagBadge lines 52-62 + its placement in CardHeader lines 213-221 — the sibling-badge slot; Badge import from ui/badge)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "frontend/components/DiagnosisCard.tsx (modified — STRETCH-01)" (verbatim badge skeleton)
  </read_first>
  <action>
    Add `confidence: number | null;` to the SearchResponseOk interface in frontend/lib/api.ts (1:1 with the new backend field, same discipline used for drift_state). In DiagnosisCard.tsx, render a sibling Badge next to VersionTagBadge in the same CardHeader row: when `response.confidence != null`, show `<Badge variant="outline" ...>{Math.round(response.confidence * 100)}% confidence</Badge>` (mono, muted, shrink-0), following the existing Badge shape; when confidence is null, render nothing (no badge). Do not alter the root-cause headline, evidence list, or Accept/Dismiss controls. No CSS-file edits (Tailwind utilities only; B-04 not in scope here but keep the discipline).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && grep -q 'confidence' lib/api.ts && grep -q 'confidence' components/DiagnosisCard.tsx && npx tsc --noEmit && echo CONFIDENCE_UI_OK</automated>
    <human-check>With backend running, search "customers double-charged" in the browser: the diagnosis card shows an "N% confidence" badge beside the version tag; an off-corpus/no-results query shows no badge (and no crash).</human-check>
  </verify>
  <acceptance_criteria>
    - SearchResponseOk has confidence: number | null; DiagnosisCard renders the percentage badge only when confidence != null — command prints CONFIDENCE_UI_OK and tsc passes.
    - Live: the confidence badge appears for a grounded search and is absent (no crash) when confidence is null / no_results.
  </acceptance_criteria>
  <done>Confidence percentage badge renders on grounded diagnoses; typed 1:1 to the backend field; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /search | Existing endpoint; verbose=True changes the internal per-dataset result shape server-side only |
| backend → client (confidence) | Only a scalar confidence crosses; raw verbose objects_result must not be forwarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | verbose objects_result | low | mitigate | Extract only a scalar confidence (and the existing trimmed evidence excerpts); never forward the raw objects_result/ScoredResult payloads to the client |
| T-04-12 | Error Handling | confidence parse failure | low | mitigate | Best-effort extraction (confidence=None on any parse issue), mirroring the qa_id best-effort pattern; never fails /search or leaks exception text |
</threat_model>

<verification>
- pytest test_search_helpers.py green (old + new tests); CONFIDENCE_WIRED + CONFIDENCE_UI_OK; tsc clean.
- Live: confidence badge on grounded search, absent on no_results, no crash.
</verification>

<success_criteria>
- STRETCH-01 delivered: a real, normalized confidence from the CHUNKS retriever shown as a badge on the diagnosis card, best-effort and fully tested.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| verbose CHUNKS call + confidence key | search.py edit | POST /search now returns flat "confidence" (number|null) |
| _confidence_from_results | helper | pure score-normalization helper (higher=better) |
| _flatten_and_truncate (extended) | helper | now reads objects_result with search_result backward-compat |
| confidence field | api.ts edit | SearchResponseOk.confidence: number \| null |
| confidence badge | DiagnosisCard.tsx edit | percentage badge beside VersionTagBadge |
| new unit tests | test_search_helpers.py edit | objects_result path + _confidence_from_results |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-05-SUMMARY.md` when done.
</output>
