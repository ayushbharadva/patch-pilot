---
phase: 04-demo-loop-stretch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seed/incidents/queue-backlog-incident.md
  - seed/workarounds_v1_8/dedup-monitoring-note.md
  - seed/workarounds_v1_9/idempotency-rollout-note.md
  - patchpilot_memory.snapshot.tar
autonomous: true
requirements: [GRAPH-01, DEMO-01]
must_haves:
  truths:
    - "Re-seeding ingests 11 docs (8 original + 3 new) across the three locked datasets without breaking the Stripe double-charged arc"
    - "The before/after forget flip still produces a different answer (FLIP OK) and incidents survives (INCIDENTS SURVIVED) after enrichment"
    - "A fresh patchpilot_memory.snapshot.tar exists that contains workarounds_v1_8 and restores cleanly"
  artifacts:
    - "seed/incidents/queue-backlog-incident.md (new decoy)"
    - "seed/workarounds_v1_8/dedup-monitoring-note.md (new, reinforces dedup_sweeper)"
    - "seed/workarounds_v1_9/idempotency-rollout-note.md (new, reinforces idempotency_guard)"
    - "patchpilot_memory.snapshot.tar (re-captured post-enrichment, contains workarounds_v1_8)"
  key_links:
    - "seed_cli.py folder glob auto-discovers the 3 new docs — no seed_cli code change needed"
    - "scripts/snapshot_memory.py --save captures the enriched state that backend/reset.py (Plan 02) restores"
---

<objective>
Enrich the seed corpus with 3 isolated documents so the 3D memory graph (Plan 03) has visible density, then re-capture the demo-reset snapshot (Plan 02's restore payload) AFTER enrichment so it contains `workarounds_v1_8`. This is the data foundation both downstream slices consume; it runs the single `cognify()` bill for the phase.

Purpose: D-09 (denser graph = GRAPH-01 scoring showpiece) + B-03 (the committed snapshot predates the Phase-1 flip and cannot restore `workarounds_v1_8`; reset needs a fresh post-enrichment snapshot).
Output: 3 new seed docs + a fresh `patchpilot_memory.snapshot.tar` verified to round-trip and contain `workarounds_v1_8`.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full search to release to drift to forget to re-search loop in under 120 seconds, reset the demo to a clean state in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

> NOTE for orchestrator: the ROADMAP Phase 4 Goal line is prose, not strict `As a / I want to / so that` form. The story above was derived faithfully from that goal. If a canonical story is required, run `/gsd mvp-phase 4`.

**Slice outcome after this plan:** re-running `seed_cli.py --reset` rebuilds a denser corpus, and a fresh snapshot exists that Plan 02's reset button can restore to a clean, `workarounds_v1_8`-present demo state.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-CONTEXT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
@seed/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 3 isolated enrichment docs (D-09)</name>
  <files>seed/incidents/queue-backlog-incident.md, seed/workarounds_v1_8/dedup-monitoring-note.md, seed/workarounds_v1_9/idempotency-rollout-note.md</files>
  <read_first>
    - seed/README.md (the isolation rule and document inventory — source of truth for which proper nouns belong to which dataset)
    - seed/incidents/login-timeout-incident.md and seed/incidents/api-latency-spike-incident.md (existing decoy structure/tone/length ~172-220 words)
    - seed/workarounds_v1_8/nightly-dedup-workaround.md (existing dedup_sweeper / nightly-dedup-cron doc — the entity to reinforce, not to redefine)
    - seed/workarounds_v1_9/release-v1.9.md and seed/workarounds_v1_9/idempotency-fix-thread.md (existing idempotency_guard docs — the entity to reinforce)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "Seed corpus enrichment (D-09)" (recommended count + isolation constraint)
  </read_first>
  <action>
    Add exactly 3 new markdown docs (~172-220 words each, matching existing tone), one per existing seed folder, without touching the arc-critical docs. Per D-09 + PATTERNS recommended count (1 decoy incident, 1 reinforcing v1_8, 1 reinforcing v1_9):
    (a) seed/incidents/queue-backlog-incident.md — a NEW unrelated decoy incident (e.g. background-job queue backlog / worker starvation). Must NOT mention "double-charged", "duplicate charge", or Stripe, and must NOT name dedup_sweeper, nightly-dedup-cron, or idempotency_guard. Any new proper noun (a queue/worker component name) must be exclusive to this file. Thickens the incidents graph without polluting the arc.
    (b) seed/workarounds_v1_8/dedup-monitoring-note.md — a monitoring/ops note that REINFORCES the existing isolated dedup_sweeper / nightly-dedup-cron entities. Must name dedup_sweeper and must NOT name idempotency_guard. Do not introduce a competing new fix entity.
    (c) seed/workarounds_v1_9/idempotency-rollout-note.md — a rollout/verification note that REINFORCES the existing isolated idempotency_guard entity. Must name idempotency_guard and must NOT name dedup_sweeper or nightly-dedup-cron.
    Keep the shared vocabulary ("customers double-charged", "Stripe webhook retries") ONLY in the two workaround reinforcement docs so they stay tied into the arc; the incidents decoy stays fully unrelated. Preserves DEMO-02 and the Cognee #1023 isolation invariant (RESEARCH.md Anti-Pattern: do not add new cross-dataset entity names during enrichment). Do NOT modify seed_cli.py — its folder glob auto-discovers new files.
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && grep -q dedup_sweeper seed/workarounds_v1_8/dedup-monitoring-note.md && ! grep -q idempotency_guard seed/workarounds_v1_8/dedup-monitoring-note.md && grep -q idempotency_guard seed/workarounds_v1_9/idempotency-rollout-note.md && ! grep -q dedup_sweeper seed/workarounds_v1_9/idempotency-rollout-note.md && ! grep -Eiq 'double-charged|idempotency_guard|dedup_sweeper' seed/incidents/queue-backlog-incident.md && echo ISOLATION_OK</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && git status --porcelain seed/workarounds_v1_8/nightly-dedup-workaround.md seed/workarounds_v1_9/release-v1.9.md seed/incidents/stripe-double-charge-incident.md seed/incidents/stripe-double-charge-escalation.md | grep -q . && echo ARC_DOCS_MODIFIED_FAIL || echo ARC_DOCS_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 3 new .md files exist, one in each of seed/incidents, seed/workarounds_v1_8, seed/workarounds_v1_9.
    - dedup_sweeper appears in the new v1_8 doc and NOT in the new v1_9 doc; idempotency_guard appears in the new v1_9 doc and NOT in the new v1_8 doc (isolation preserved) — first command prints ISOLATION_OK.
    - The new incidents decoy contains none of: double-charged, dedup_sweeper, idempotency_guard.
    - The 4 arc-critical existing docs are unmodified — second command prints ARC_DOCS_UNTOUCHED.
  </acceptance_criteria>
  <done>3 isolated enrichment docs authored; arc + #1023 isolation invariant preserved; existing arc docs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-seed once + capture fresh post-enrichment snapshot (B-03)</name>
  <files>patchpilot_memory.snapshot.tar</files>
  <read_first>
    - scripts/snapshot_memory.py (save/restore/snapshot_exists — the reset engine Plan 02 reuses; MEMORY_ROOT + SNAPSHOT_PATH resolution)
    - seed/seed_cli.py (reset = restore-if-snapshot-exists ELSE prune_data+prune_system+seed; seed = add + per-dataset cognify; flip = search then forget(workarounds_v1_8) then re-search, exit 0 on FLIP OK + INCIDENTS SURVIVED)
    - .planning/STATE.md section "Blockers/Concerns" (B-03: committed snapshot predates the flip; reseed bills cognify on Mistral free tier)
    - .claude/CLAUDE.md section "Budget" (keep cognify calls minimal — this is the phase's single reseed)
  </read_first>
  <action>
    Rebuild a CLEAN enriched memory state, then capture the snapshot BEFORE any forget mutates it. Because the stale committed snapshot exists, `seed_cli.py --reset` would restore it instead of reseeding — so first remove the stale snapshot, then let `--reset` fall through to its prune+reseed branch (which now auto-discovers the 3 new docs). Exact sequence (Windows venv `.venv/Scripts/python.exe`, run from repo root):
    1. Delete the stale patchpilot_memory.snapshot.tar.
    2. Run `seed/seed_cli.py --reset` — with no snapshot present this runs prune_data() + prune_system() then seed(), billing cognify() ONCE for the enriched 11-doc corpus.
    3. Run `scripts/snapshot_memory.py --save` — captures the clean, pre-flip, enriched state (workarounds_v1_8 present).
    4. Run `seed/seed_cli.py --flip` — confirms the arc still flips post-enrichment (mutates memory: forgets workarounds_v1_8). Must exit 0 (FLIP OK + INCIDENTS SURVIVED).
    5. Run `scripts/snapshot_memory.py --restore` — brings workarounds_v1_8 back, leaving memory clean and proving the snapshot round-trips (the exact path backend/reset.py drives in Plan 02).
    The backend must NOT be running during this (single-worker Kuzu file lock; the CLI process needs the lock). Do NOT run `--flip` before `--save`.
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && rm -f patchpilot_memory.snapshot.tar && .venv/Scripts/python.exe seed/seed_cli.py --reset && .venv/Scripts/python.exe scripts/snapshot_memory.py --save && .venv/Scripts/python.exe seed/seed_cli.py --flip && .venv/Scripts/python.exe scripts/snapshot_memory.py --restore</automated>
    <automated>cd "F:/Ayush Bharadva/patch-pilot" && .venv/Scripts/python.exe -c "import tarfile; names=tarfile.open('patchpilot_memory.snapshot.tar').getnames(); assert any('workarounds_v1_8' in n for n in names), 'v1_8 missing from snapshot'; print('SNAPSHOT_HAS_V1_8')"</automated>
  </verify>
  <acceptance_criteria>
    - `seed_cli.py --reset` completes with SEED OK (11 docs across incidents/workarounds_v1_8/workarounds_v1_9).
    - `seed_cli.py --flip` exits 0, printing both FLIP OK and INCIDENTS SURVIVED (arc preserved post-enrichment; B-01 loop-survives-forget still holds).
    - `patchpilot_memory.snapshot.tar` exists and its tar member list includes a `workarounds_v1_8` path — second command prints SNAPSHOT_HAS_V1_8.
    - After `--restore`, memory is in the clean enriched pre-flip state (workarounds_v1_8 present again).
  </acceptance_criteria>
  <done>Single reseed run; fresh snapshot captured pre-flip, verified to contain workarounds_v1_8 and to round-trip via restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed author → Cognee ingest (cognify) | Human-authored markdown becomes graph entities; cross-dataset entity names would reopen #1023 |
| memory tree → snapshot tar (filesystem) | Snapshot may contain cognified seed content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | new seed docs | medium | mitigate | Isolation grep gate (Task 1 verify) blocks any new cross-dataset entity name (dedup_sweeper vs idempotency_guard vs decoy), preserving Cognee #1023 isolation |
| T-04-02 | Information Disclosure | patchpilot_memory.snapshot.tar | low | accept | `*.snapshot.tar` is already gitignored (.gitignore line 15); the tar is never committed — no code change needed |
</threat_model>

<verification>
- Both Task 1 grep gates print ISOLATION_OK and ARC_DOCS_UNTOUCHED.
- `--reset` → SEED OK; `--flip` → FLIP OK + INCIDENTS SURVIVED (exit 0); snapshot member list includes workarounds_v1_8.
- After the sequence, `.patchpilot_memory/` is a clean enriched pre-flip state and a fresh snapshot exists on disk (gitignored).
</verification>

<success_criteria>
- 3 isolated enrichment docs exist; arc-critical docs untouched; #1023 isolation preserved.
- Fresh `patchpilot_memory.snapshot.tar` contains `workarounds_v1_8` and restores cleanly — the reset payload Plan 02 depends on.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| seed/incidents/queue-backlog-incident.md | seed doc | new isolated decoy incident |
| seed/workarounds_v1_8/dedup-monitoring-note.md | seed doc | reinforces dedup_sweeper (v1_8-only) |
| seed/workarounds_v1_9/idempotency-rollout-note.md | seed doc | reinforces idempotency_guard (v1_9-only) |
| patchpilot_memory.snapshot.tar | artifact (gitignored) | re-captured post-enrichment, contains workarounds_v1_8; restore payload for backend/reset.py |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-01-SUMMARY.md` when done.
</output>
