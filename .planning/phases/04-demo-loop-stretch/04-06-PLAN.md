---
phase: 04-demo-loop-stretch
plan: 06
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - frontend/components/HealthDashboard.tsx
  - frontend/components/IncidentTimeline.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [STRETCH-02, STRETCH-03]
must_haves:
  truths:
    - "A memory health dashboard shows 🟢/🟡/🔴 counts aggregated from the live dataset list"
    - "An incident timeline shows incidents/releases chronologically (STRETCH-03 — the first feature to cut if time-boxed)"
    - "Both mount on the main page without a redundant fetch (shared react-query cache) and without a PostCSS/build error"
  artifacts:
    - "frontend/components/HealthDashboard.tsx (STRETCH-02)"
    - "frontend/components/IncidentTimeline.tsx (STRETCH-03, cuttable)"
    - "placement in frontend/app/page.tsx"
  key_links:
    - "HealthDashboard shares DATASETS_QUERY_KEY with DatasetList so counts always agree and no extra fetch fires"
    - "Both are pure client-side aggregation of GET /datasets — no new backend endpoint (RESEARCH Architectural Responsibility Map)"
---

<objective>
Ship the last two stretch depth features, gated behind Plan 04's confirmed loop (D-10). STRETCH-02: a memory-health dashboard aggregating the Phase-3 drift-state counts (🟢/🟡/🔴). STRETCH-03: an incident timeline ordering incidents/releases chronologically — the highest-effort, first-to-cut slice (D-10 reverse cut order). Both are pure client-side aggregation of the existing GET /datasets response; no new backend endpoint (RESEARCH.md Architectural Responsibility Map).

Purpose: STRETCH-02 + STRETCH-03 (D-10 — build after loop confirmed; cut STRETCH-03 first if time-boxed).
Output: HealthDashboard + IncidentTimeline components placed on the main page.
</objective>

## Phase Goal (user story)

**As a** demo operator presenting PatchPilot to judges, **I want to** run the full loop under 120 seconds, reset the demo in one click, and show the live Cognee knowledge graph, **so that** judges see Cognee's memory lifecycle working live, repeatably, and provably backed by a real graph.

**Slice outcome after this plan:** a real user sees at-a-glance memory-health counts and a chronological incident timeline — added demo depth on top of the confirmed loop.

> **Cut order (D-10):** Task 2 (IncidentTimeline / STRETCH-03) is the first feature to cut if time-boxed — highest-effort, lowest-precedent slice. Task 1 (HealthDashboard / STRETCH-02) is cheaper and reuses existing drift data. If time-constrained, ship Task 1 and skip Task 2.

<execution_context>
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/workflows/execute-plan.md
@F:/Ayush Bharadva/patch-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-demo-loop-stretch/04-RESEARCH.md
@.planning/phases/04-demo-loop-stretch/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory health dashboard (STRETCH-02)</name>
  <files>frontend/components/HealthDashboard.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/components/DatasetList.tsx (DATASETS_QUERY_KEY export line 17; DRIFT_BADGE color/label map lines 22-26; useQuery({queryKey:DATASETS_QUERY_KEY, queryFn:listDatasets}) lines 155-158; Card/CardHeader/CardContent shell)
    - frontend/lib/api.ts (DatasetInfo + DriftState types lines 300-320 — the data already fetched)
    - frontend/app/page.tsx (section-per-feature layout — where the dashboard mounts; note the search/graph toggle from Plan 03 already exists here)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "frontend/components/HealthDashboard.tsx (new — STRETCH-02)" (verbatim skeleton + cache-sharing note)
  </read_first>
  <action>
    Create frontend/components/HealthDashboard.tsx ("use client") as pure client-side aggregation — NO new backend endpoint. Read the dataset list via useQuery with queryKey DATASETS_QUERY_KEY (imported from DatasetList.tsx) and queryFn listDatasets so this shares the exact cache (counts always agree with the list; no redundant fetch). Tally counts by drift_state into {stable, aging, drifting} and render each with its 🟢/🟡/🔴 label (reuse the DRIFT_BADGE label map — duplicate it or export+import from DatasetList) inside a Card titled "Memory Health". Handle the empty/loading case gracefully (data may be undefined). Mount HealthDashboard in frontend/app/page.tsx under the search view (same section/aria-label convention). Tailwind utilities only — no CSS-file edits, no `*/` inside any CSS comment (B-04).
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && test -f components/HealthDashboard.tsx && grep -q DATASETS_QUERY_KEY components/HealthDashboard.tsx && grep -q HealthDashboard app/page.tsx && npx tsc --noEmit && echo HEALTH_OK</automated>
    <human-check>With backend running, load http://localhost:3000: a "Memory Health" card shows 🟢/🟡/🔴 counts; after uploading a release (which flips workarounds_v1_8 to drifting) the drifting count increases; after Reset the counts return to the clean baseline. Page loads with no PostCSS error (B-04).</human-check>
  </verify>
  <acceptance_criteria>
    - frontend/components/HealthDashboard.tsx exists, uses DATASETS_QUERY_KEY (shared cache, no extra fetch), tallies drift_state counts, and is mounted in page.tsx — command prints HEALTH_OK and tsc passes.
    - Live: counts reflect the live dataset list and update after upload/forget/reset; no PostCSS error.
  </acceptance_criteria>
  <done>Health dashboard shows live 🟢/🟡/🔴 counts via the shared dataset cache; browser-verified clean.</done>
</task>

<task type="auto">
  <name>Task 2: Incident timeline (STRETCH-03 — FIRST TO CUT if time-boxed)</name>
  <files>frontend/components/IncidentTimeline.tsx, frontend/app/page.tsx</files>
  <read_first>
    - frontend/components/DatasetList.tsx (Card + row-list shell lines 160-184; loading/error/empty states — the closest available shape; there is NO timeline precedent in the repo)
    - frontend/lib/api.ts (DatasetInfo shape from GET /datasets — the only chronological metadata available client-side without a new endpoint)
    - frontend/app/page.tsx (where the timeline section mounts)
    - .planning/phases/04-demo-loop-stretch/04-PATTERNS.md section "frontend/components/IncidentTimeline.tsx (new — STRETCH-03...)" (weakest analog note; cut-first per D-10) + RESEARCH.md Architectural Responsibility Map row "Incident timeline" (chronological sort of dataset/doc metadata; no new lifecycle verb)
  </read_first>
  <action>
    ONLY build this if time allows after Task 1 (D-10: STRETCH-03 is the first cut). Create frontend/components/IncidentTimeline.tsx ("use client") rendering incidents/releases chronologically from the GET /datasets response (reuse the shared DATASETS_QUERY_KEY cache) — order the datasets by their version/recency (workarounds_v{N} version ordering already exists conceptually via the version regex; incidents anchor the baseline). Use the same Card/CardHeader/CardContent shell + loading/error/empty states as DatasetList. Keep it read-only depth (no new mutations). Mount IncidentTimeline in frontend/app/page.tsx under the search view. Tailwind utilities only; no CSS-file edits, no `*/` in CSS comments (B-04). If cut, do NOT create the file and do NOT reference it in page.tsx — leave Task 1's page.tsx edit intact.
  </action>
  <verify>
    <automated>cd "F:/Ayush Bharadva/patch-pilot/frontend" && { test -f components/IncidentTimeline.tsx && grep -q IncidentTimeline app/page.tsx && npx tsc --noEmit && echo TIMELINE_OK; } || echo TIMELINE_CUT</automated>
    <human-check>If built: with backend running, the main page shows an incident timeline ordering incidents/releases chronologically; page loads with no PostCSS error (B-04). If cut per D-10, this check is N/A (record the cut decision in the SUMMARY).</human-check>
  </verify>
  <acceptance_criteria>
    - If built: IncidentTimeline.tsx exists, renders chronological entries from the shared /datasets cache, is mounted in page.tsx, tsc passes — command prints TIMELINE_OK; browser shows the timeline with no PostCSS error.
    - If cut (time-boxed per D-10): the file is absent, page.tsx has no dangling reference, Task 1 remains intact and shippable — command prints TIMELINE_CUT and the SUMMARY records the cut.
  </acceptance_criteria>
  <done>Incident timeline shipped chronologically, OR explicitly cut per D-10 with page.tsx left consistent and the cut recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client aggregation → GET /datasets | Reuses an existing endpoint already displayed by DatasetList; no new server surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Information Disclosure | HealthDashboard / IncidentTimeline data | low | accept | Both re-aggregate GET /datasets data already rendered by DatasetList; no new data exposure, no new backend endpoint |
| T-04-14 | DoS / client crash | new component styling (CSS) | medium | mitigate | Tailwind utilities only; no `*/` inside CSS comments (B-04 crashes the whole frontend); the human-check live browser load is the only reliable catch |
</threat_model>

<verification>
- HEALTH_OK; TIMELINE_OK or TIMELINE_CUT; tsc clean.
- Live: health counts update with upload/forget/reset; timeline (if built) renders chronologically; no PostCSS error.
</verification>

<success_criteria>
- STRETCH-02 delivered: live memory-health counts via the shared dataset cache.
- STRETCH-03 delivered OR explicitly cut per D-10 with page.tsx left consistent and shippable.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| frontend/components/HealthDashboard.tsx | component | 🟢/🟡/🔴 counts from shared DATASETS_QUERY_KEY cache |
| frontend/components/IncidentTimeline.tsx | component | chronological incidents/releases (cuttable per D-10) |
| HealthDashboard + IncidentTimeline placement | page.tsx edit | mounted under the search view |

<output>
Create `.planning/phases/04-demo-loop-stretch/04-06-SUMMARY.md` when done.
</output>
