---
phase: 02-core-recall
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/package.json
  - frontend/components.json
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/app/page.tsx
  - frontend/lib/api.ts
  - frontend/lib/providers.tsx
  - frontend/components/SearchBar.tsx
  - frontend/components/DiagnosisCard.tsx
autonomous: false
requirements: [RECALL-01, RECALL-02, RECALL-03]
user_setup: []
must_haves:
  truths:
    - "A user opens the dashboard in the browser, types a bug, clicks Search, and sees a diagnosis card with the root cause on top and the evidence tickets below (RECALL-03, D-06)"
    - "The card renders the GRAPH_COMPLETION root cause AND its CHUNKS evidence fused into one view (RECALL-01, RECALL-02)"
    - "Evidence snippets are truncated (2-3 shown) and click-to-expand to full source text (D-07, D-08)"
    - "A muted mono version tag derived from source_dataset renders in the card's D-09 slot, built to accept a 🟢/🟡/🔴 state later without layout change"
    - "Before the first search the empty state shows the prompt plus a clickable example-query chip 'customers double-charged' (D-19)"
    - "While the search is in flight a skeleton card (not a bare spinner) matches the real card's shape so there is no layout shift (D-20)"
    - "A zero-result search shows the explicit 'No prior incidents found for this query' message (D-21)"
    - "A search error renders a short human message, never raw exception text (D-24)"
  artifacts:
    - path: frontend/app/layout.tsx
      provides: "Root layout with Space Grotesk / Inter / IBM Plex Mono font variables + React Query provider"
      contains: "Space_Grotesk"
    - path: frontend/lib/api.ts
      provides: "Typed searchIncident() fetch wrapper mirroring backend /search contract"
      contains: "searchIncident"
    - path: frontend/components/SearchBar.tsx
      provides: "Persistent top search bar + example-query chip (D-18, D-19)"
    - path: frontend/components/DiagnosisCard.tsx
      provides: "Diagnosis card: root cause, evidence, version tag, skeleton, empty state (D-06..D-09, D-20, D-21)"
    - path: frontend/app/page.tsx
      provides: "Single-page dashboard shell hosting search + results region (D-17)"
  key_links:
    - from: frontend/components/SearchBar.tsx
      to: frontend/lib/api.ts
      via: "React Query mutation calls searchIncident(query)"
      pattern: "searchIncident"
    - from: frontend/lib/api.ts
      to: backend/search.py
      via: "fetch POST to `${API_BASE}/search` with {query}"
      pattern: "/search"
    - from: frontend/components/DiagnosisCard.tsx
      to: frontend/lib/api.ts
      via: "renders the SearchResponse union (ok / no_results / error)"
      pattern: "source_dataset|no_results"
---

<objective>
Scaffold the project's first frontend (Next.js 16 App Router + shadcn/ui + the three locked fonts + React Query) and deliver the search vertical slice end-to-end in the browser: a persistent search bar and a diagnosis card that renders the fused root-cause + evidence payload from Plan 01's `/search`, with skeleton, empty, and zero-result states.

Purpose: This is the signature UI element of PatchPilot — the evidence-grounded diagnosis card. After this plan a user can actually search incident memory in the browser and see a grounded answer, which is the heart of the phase's user story.
Output: A running Next.js dashboard, typed API client, SearchBar, and DiagnosisCard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-recall/02-UI-SPEC.md
@.planning/phases/02-core-recall/02-RESEARCH.md
@.planning/phases/02-core-recall/02-PATTERNS.md
@.planning/phases/02-core-recall/02-CONTEXT.md
@backend/search.py
</context>

## Phase Goal

**As an** engineer, **I want to** ingest incident history, search a bug, and get an evidence-grounded diagnosis I can reinforce, **so that** I recall how similar incidents were fixed before instead of re-debugging from scratch.

_This plan delivers the browser-visible search slice: type a bug → see a grounded diagnosis card._

## Artifacts this phase produces (Plan 02)

New symbols/paths created here (excluded from downstream drift verification):
- `frontend/` project scaffold (create-next-app: `package.json`, `tsconfig.json`, `next.config`, `tailwind` config, `app/globals.css`), `frontend/components.json` (shadcn)
- `frontend/app/layout.tsx` — root layout; font CSS variables `--font-space-grotesk`, `--font-inter`, `--font-ibm-plex-mono`
- `frontend/lib/providers.tsx` — `Providers` client component wrapping React Query `QueryClientProvider`
- `frontend/lib/api.ts` — `API_BASE` const; `SearchResponse` type union; `searchIncident(query: string)` async function; `EvidenceSnippet` type
- `frontend/components/SearchBar.tsx` — `SearchBar` client component
- `frontend/components/DiagnosisCard.tsx` — `DiagnosisCard` + `DiagnosisCardSkeleton`; `versionTagFromDataset(name)` helper
- `frontend/app/page.tsx` — dashboard page; search + results region
- shadcn blocks added: card, button, input, badge, collapsible, skeleton

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + shadcn + fonts + React Query + typed API client + dashboard shell with the search bar</name>
  <files>frontend/package.json, frontend/components.json, frontend/app/layout.tsx, frontend/app/globals.css, frontend/app/page.tsx, frontend/lib/api.ts, frontend/lib/providers.tsx, frontend/components/SearchBar.tsx</files>
  <read_first>
    - .planning/phases/02-core-recall/02-UI-SPEC.md § "Design System" (executor init sequence), § "Typography" (fonts + sizes/weights), § "Color" (light + dark anchors, accent reserved list), § "Copywriting Contract" (Search CTA, empty-state copy), § "Component Inventory" (SearchBar primitives)
    - .planning/phases/02-core-recall/02-RESEARCH.md § "Standard Stack / Installation" (exact create-next-app + shadcn + react-query commands, verified versions) and § "Recommended Project Structure"
    - .planning/phases/02-core-recall/02-PATTERNS.md § "Frontend files" (lib/api.ts must mirror backend JSON shapes exactly)
    - backend/search.py (the exact response shape to mirror: {status, root_cause, evidence[{excerpt,full_text,source}], source_dataset, session_id, qa_id} / {status:"no_results"} / {status:"error", message})
  </read_first>
  <action>
    In a new frontend/ directory, run create-next-app with the typescript, tailwind, app-router, and turbopack flags; then `npx shadcn init --base-color neutral` accepting defaults (wires CSS variables for light + dark); then install @tanstack/react-query. Add the shadcn blocks needed this plan: card, button, input, badge, collapsible, skeleton.

    Wire the three locked fonts via next/font/google in layout.tsx — Space_Grotesk, Inter, IBM_Plex_Mono — each exposed as a CSS variable (--font-space-grotesk, --font-inter, --font-ibm-plex-mono) and mapped to Tailwind font-family utilities (display/body/mono) so headings use Space Grotesk, body uses Inter, and technical strings use IBM Plex Mono. Set weights to exactly 400 and 600 (no 300/500/700). Confirm the shadcn light/dark token anchors match the UI-SPEC color table (page bg #f8fafc / #0d1117; card #ffffff / #161b22; accent indigo-600 / indigo-500).

    Create frontend/lib/providers.tsx as a "use client" component that instantiates a QueryClient and wraps children in QueryClientProvider; mount it in layout.tsx around the app body.

    Create frontend/lib/api.ts: export API_BASE read from NEXT_PUBLIC_API_BASE_URL with a default of http://localhost:8000; export an EvidenceSnippet type ({excerpt, full_text, source}); export a SearchResponse discriminated union matching backend exactly ({status:"ok", root_cause, evidence: EvidenceSnippet[], source_dataset, session_id, qa_id} | {status:"no_results"} | {status:"error", message}); export async searchIncident(query) POSTing JSON {query} to `${API_BASE}/search` and returning the typed union. Keep client and server payload shapes in lockstep.

    Build frontend/app/page.tsx as the single-page dashboard (D-17): a persistent top region rendering SearchBar, and a results region below it. Manage the current SearchResponse (or loading) state at the page level and pass it down. Render the empty state (D-19) before any search — heading "Search your incident memory" and a body prompt naming the example query in mono. Stack sections with the xl (32px) gap from the spacing scale.

    Create frontend/components/SearchBar.tsx ("use client"): a persistent top bar (D-18) with an Input and a primary accent "Search" button, plus a clickable example-query chip below labeled with the canonical mono query customers double-charged (D-19) that fills the input and submits. Use a React Query mutation wrapping searchIncident; on states drive the page: pending → loading, success ok → card, success no_results → empty message, success/thrown error → short inline error message (D-24). Do not render results here — SearchBar only owns the input + chip and lifts the response to the page.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` succeeds and `npx tsc --noEmit` reports no errors
    - `grep -n 'Space_Grotesk' frontend/app/layout.tsx` and `grep -n 'IBM_Plex_Mono' frontend/app/layout.tsx` both present; the QueryClientProvider is mounted
    - `grep -n 'searchIncident' frontend/lib/api.ts` exports the wrapper; the SearchResponse union has all three variants (ok / no_results / error) with fields matching backend/search.py
    - `grep -n 'customers double-charged' frontend/components/SearchBar.tsx` shows the example chip; the Search button and chip both call the same submit handler
    - The empty-state heading "Search your incident memory" renders before any search (grep page.tsx or SearchBar)
  </acceptance_criteria>
  <done>The Next.js app builds and boots; fonts + React Query are wired; the typed api client mirrors the backend contract; a user sees the empty state and can submit a search from the persistent top bar or the example chip.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnosis card — root cause, expandable evidence, version tag, skeleton, and zero-result state</name>
  <files>frontend/components/DiagnosisCard.tsx, frontend/app/page.tsx</files>
  <read_first>
    - .planning/phases/02-core-recall/02-UI-SPEC.md § "DiagnosisCard" row (shadcn primitives Card/Badge/Collapsible/Skeleton), § "Typography" (Display 28/600 Space Grotesk for the headline; Body 16/400 Inter for prose/excerpts; mono 14 for the tag), § "Color" (drift-badge palette reserved for Phase 3 — Phase 2 tag is neutral/muted), § "Copywriting Contract" (zero-result + loading rows)
    - .planning/phases/02-core-recall/02-CONTEXT.md D-06, D-07, D-08, D-09, D-20, D-21
    - frontend/lib/api.ts (the SearchResponse union + EvidenceSnippet type this card renders)
    - frontend/app/page.tsx (how the page holds/loading state and where the card mounts)
  </read_first>
  <action>
    Create frontend/components/DiagnosisCard.tsx rendering a status-ok SearchResponse: the root-cause text as the top headline using the Display style (Space Grotesk 28/600) per D-06, then the evidence tickets stacked below it. Render 2-3 evidence snippets (D-07) each as a short truncated excerpt inside a shadcn Collapsible whose trigger is a 44px-hit-target chevron that expands to reveal the snippet's full_text (D-08); label each with its source. Render a small muted version tag in the D-09 slot derived from source_dataset via a versionTagFromDataset helper (e.g. workarounds_v1_8 → v1.8, incidents → a neutral label) styled in IBM Plex Mono; implement it as a Badge that accepts an optional health-state prop (stable/aging/drifting) it does not use yet, so Phase 3 can color it without changing layout. Keep the tag neutral/muted in Phase 2.

    Export a DiagnosisCardSkeleton matching the real card's dimensions (a headline block + 2-3 evidence rows) for the loading state (D-20) — never a bare spinner; if the Plan 01 measured latency exceeds ~5s, add a subtle "Searching memory…" label beneath the skeleton. Render the zero-result message "No prior incidents found for this query" for status no_results (D-21). Do NOT add Accept/Dismiss controls yet — those land in Plan 04.

    In page.tsx, mount DiagnosisCard (and its Skeleton while pending) in the results region, driven by the page-level state from SearchBar.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Build + tsc pass
    - `grep -n 'versionTagFromDataset' frontend/components/DiagnosisCard.tsx` derives the tag from source_dataset; the Badge accepts an (unused) health-state prop
    - `grep -n 'Collapsible' frontend/components/DiagnosisCard.tsx` wraps evidence excerpts with full_text expansion
    - `grep -n 'No prior incidents found for this query' frontend/components/DiagnosisCard.tsx` renders the D-21 message
    - A DiagnosisCardSkeleton export exists and is used for the pending state (grep page.tsx or DiagnosisCard)
    - No Accept/Dismiss/Reinforced controls exist in this file yet (deferred to Plan 04)
  </acceptance_criteria>
  <done>The diagnosis card renders root cause on top with expandable evidence below, a neutral mono version tag in the Phase-3-ready slot, a shape-matched skeleton while loading, and the explicit zero-result message.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the search slice end-to-end in the browser</name>
  <files>frontend/app/page.tsx, frontend/components/SearchBar.tsx, frontend/components/DiagnosisCard.tsx</files>
  <action>Pause for the developer to visually confirm the search slice (empty state, skeleton, diagnosis card with root cause + expandable evidence + version tag, zero-result message) works end-to-end against the live backend. Do not proceed until the developer signals approval or lists issues; if issues are reported, fix them and re-present. This is a verification gate, not a code-writing step — all implementation was completed in Tasks 1-2.</action>
  <verify><human-check>Developer confirms the search slice renders and behaves per the steps below.</human-check></verify>
  <done>Developer types "approved" (or issues are fixed and re-approved).</done>
  <what-built>The full search slice in the browser: a single-page dashboard with a persistent search bar, an example-query chip, and a diagnosis card that renders the fused root cause + expandable evidence + version tag from the live backend, with skeleton / empty / zero-result states.</what-built>
  <how-to-verify>
    1. Start the backend: `cd backend && ../.venv/bin/uvicorn main:app --workers 1 --host 127.0.0.1` (restore seed memory first if needed: `.venv/bin/python seed/seed_cli.py --reset`).
    2. Start the frontend: `cd frontend && npm run dev`, open http://localhost:3000.
    3. Before searching, confirm the empty state shows "Search your incident memory" and the clickable `customers double-charged` chip.
    4. Click the chip (or type the query and press Search). Confirm a skeleton card appears (not a bare spinner), then a diagnosis card renders with the root cause on top and 2-3 evidence snippets below.
    5. Click an evidence snippet's expand chevron — confirm it reveals the full source text.
    6. Confirm the muted mono version tag (e.g. v1.8) shows next to the fix.
    7. Search a nonsense query (e.g. "asdfqwer") — confirm the "No prior incidents found for this query" message, NOT a made-up answer.
    8. Confirm fonts look right (Space Grotesk headline, Inter body, mono tag) and the layout does not shift when the skeleton swaps to real content.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party packages installed into the frontend toolchain |
| backend → browser render | Backend JSON (incl. error messages) rendered in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (create-next-app, @tanstack/react-query, shadcn blocks) | high | mitigate | All installed via official create-next-app scaffolding + shadcn CLI; @tanstack/react-query and next/react/tailwind are first-party/top-tier and were audited clean in 02-RESEARCH.md § "Package Legitimacy Audit" (no [SLOP]/[SUS]/[ASSUMED]) — so no blocking-human legitimacy checkpoint is required; pin the versions create-next-app resolves and do not hand-add unlisted packages |
| T-02-04b | Info Disclosure | DiagnosisCard / SearchBar error rendering | low | mitigate | Render only the backend's short `message` field / fixed UI copy; never render raw exception text or backend internals (D-24) |
| T-02-XSS | Tampering | evidence full_text / root_cause rendering | low | mitigate | Render backend text as React children (auto-escaped); never use dangerouslySetInnerHTML on search/evidence content |
</threat_model>

<verification>
- `cd frontend && npm run build && npx tsc --noEmit` both clean
- Human-verify checkpoint approved (search slice works end-to-end in the browser)
- api.ts SearchResponse union field-for-field matches backend/search.py's returned keys
</verification>

<success_criteria>
- A user can search incident memory in the browser and see a fused, evidence-grounded diagnosis card
- Empty (D-19), skeleton (D-20), and zero-result (D-21) states all render correctly
- Evidence is truncated with click-to-expand (D-07/D-08); the version tag occupies the Phase-3-ready D-09 slot
- No raw exception text ever reaches the DOM (D-24)
</success_criteria>

<output>
Create `.planning/phases/02-core-recall/02-02-SUMMARY.md` when done.
</output>
