---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - seed/incidents/stripe-double-charge-incident.md
  - seed/incidents/stripe-double-charge-escalation.md
  - seed/incidents/login-timeout-incident.md
  - seed/incidents/api-latency-spike-incident.md
  - seed/workarounds_v1_8/nightly-dedup-workaround.md
  - seed/workarounds_v1_8/dedup-runbook-thread.md
  - seed/workarounds_v1_9/release-v1.9.md
  - seed/workarounds_v1_9/idempotency-fix-thread.md
  - seed/README.md
autonomous: true
requirements: [DEMO-02, INGEST-03]

must_haves:
  truths:
    - "The Stripe double-charge before/after arc is authored across incidents / workarounds_v1_8 / workarounds_v1_9 (per D-01, D-02)"
    - "The v1.8 workaround's key artifact is an isolated entity that appears in NO other document, so forget(workarounds_v1_8) cleanly removes the old answer from recall (B-01)"
    - "A distinct query term ('double-charged') identifies the Stripe arc and appears in none of the decoy incidents (B-02)"
  artifacts:
    - seed/incidents/ (4 durable docs incl. 2 decoys)
    - seed/workarounds_v1_8/ (2 docs, the forgotten dataset)
    - seed/workarounds_v1_9/ (2 docs incl. release-v1.9.md)
  key_links:
    - "isolated v1.8 entity (dedup_sweeper / nightly-dedup-cron) unique to workarounds_v1_8 -> clean forget flip"
    - "isolated v1.9 entity (idempotency key on webhook handler) unique to workarounds_v1_9 -> supplies the flipped answer"
    - "durable incidents use stable shared vocab (customers double-charged / Stripe webhook retries) that survives forget"
---

<objective>
Author the DEMO-02 seed corpus: ~8 Markdown documents (D-03) telling the Stripe duplicate-charge before/after story (D-01) mapped to the locked datasets (D-02), as a mixed bag with 1-2 unrelated decoys (D-04), one file per ticket/chat/release (D-05). The entire flip depends on entity-name isolation (B-01) and distinct query terms (B-02) authored here.

Phase Goal (MVP framing): As the PatchPilot developer, I want author-controlled seed data whose entities are isolated by dataset, so that a surgical forget of workarounds_v1_8 visibly flips recall from the old dedup script to the new idempotency fix.

Purpose: The corpus is the make-or-break demo asset. If entities are not isolated, forget will not flip the answer (Pitfall 3). This plan is content-only (no code) and can run in parallel with the backend plan.
Output: `seed/incidents/`, `seed/workarounds_v1_8/`, `seed/workarounds_v1_9/` populated with 8 `.md` docs + a `seed/README.md` documenting the arc and the isolation rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- **Dataset folder = dataset name mapping (D-02, INGEST-03):**
  - `seed/incidents/` -> dataset `incidents` (durable; survives forget)
  - `seed/workarounds_v1_8/` -> dataset `workarounds_v1_8` (the dataset that gets forgotten)
  - `seed/workarounds_v1_9/` -> dataset `workarounds_v1_9` (survives; supplies the flipped answer)
- **Files (8 docs + README):**
  - `seed/incidents/stripe-double-charge-incident.md`, `stripe-double-charge-escalation.md`, `login-timeout-incident.md` (decoy), `api-latency-spike-incident.md` (decoy)
  - `seed/workarounds_v1_8/nightly-dedup-workaround.md`, `dedup-runbook-thread.md`
  - `seed/workarounds_v1_9/release-v1.9.md`, `idempotency-fix-thread.md`
  - `seed/README.md`
- **Isolated entity proper-nouns (must each appear in exactly one dataset):** v1.8 -> `dedup_sweeper` script + `nightly-dedup-cron` component (workarounds_v1_8 only); v1.9 -> idempotency-key webhook handler artifact (workarounds_v1_9 only).
- **Canonical demo query term (B-02):** "double-charged" / "duplicate charge" (Stripe arc only, absent from decoys).
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author durable incidents dataset (Stripe bug + escalation + 2 decoys) per D-01/D-04/D-05</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 arc, D-04 mixed bag, D-05 markdown; B-01/B-02 builder concerns)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 seed authoring for a clean flip; Anti-Patterns "sharing the v1.8 entity name")
  </read_first>
  <files>seed/incidents/stripe-double-charge-incident.md, seed/incidents/stripe-double-charge-escalation.md, seed/incidents/login-timeout-incident.md, seed/incidents/api-latency-spike-incident.md</files>
  <action>
    Author the durable `incidents` docs (survive forget). `stripe-double-charge-incident.md`: the bug report — customers being double-charged on Stripe webhook retries — using STABLE, SHARED vocabulary ("customers double-charged", "duplicate charge", "Stripe webhook retries") that must survive forget and remain findable. It must NOT name the v1.8 dedup artifact or the v1.9 idempotency artifact (those live only in their workaround datasets). `stripe-double-charge-escalation.md`: an escalation/chat thread about the same bug's customer impact, same shared vocabulary, still no workaround-specific proper nouns.
    Author two DECOYS (D-04, mixed bag for graph texture, only decoration — they must NOT flip): `login-timeout-incident.md` (a login/session-timeout bug) and `api-latency-spike-incident.md` (a latency/perf incident). Per B-02, the decoys must NOT contain the term "double-charged" or "duplicate charge" so the demo query stays focused on the Stripe arc.
    Keep each doc concise (~150-250 words) to stay well under the $10 cognify cap (D-03). Use realistic ticket/chat framing (title, reporter, timestamp, body).
  </action>
  <acceptance_criteria>
    - Four files exist under `seed/incidents/`
    - `stripe-double-charge-incident.md` contains "double-charged" and does NOT contain "dedup_sweeper" or "idempotency"
    - Neither decoy (`login-timeout-incident.md`, `api-latency-spike-incident.md`) contains the string "double-charged" (B-02 focus)
    - Each incident doc is under ~300 words
  </acceptance_criteria>
  <verify>
    <automated>test $(ls seed/incidents/*.md | wc -l) -ge 4 && grep -qi 'double-charged' seed/incidents/stripe-double-charge-incident.md && ! grep -qi 'dedup_sweeper' seed/incidents/stripe-double-charge-incident.md && ! grep -qi 'idempotency' seed/incidents/stripe-double-charge-incident.md && ! grep -qi 'double-charged' seed/incidents/login-timeout-incident.md && ! grep -qi 'double-charged' seed/incidents/api-latency-spike-incident.md</automated>
  </verify>
  <done>Four durable incident docs authored: the Stripe bug + escalation in stable shared vocabulary (survive forget, findable) plus two topic-distinct decoys that never mention double-charging (B-02).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the two workaround datasets with isolated entities (the forget flip) per D-02/B-01</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 old vs new fix, D-02 dataset mapping, B-01 isolated entity names)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 isolated-entity authoring; Pitfall 3 forget doesn't flip; Anti-Patterns)
    - seed/incidents/stripe-double-charge-incident.md (to guarantee the workaround proper-nouns appear in NO incident doc)
  </read_first>
  <files>seed/workarounds_v1_8/nightly-dedup-workaround.md, seed/workarounds_v1_8/dedup-runbook-thread.md, seed/workarounds_v1_9/release-v1.9.md, seed/workarounds_v1_9/idempotency-fix-thread.md</files>
  <action>
    Author `workarounds_v1_8/` (the dataset that gets forgotten, per D-02). `nightly-dedup-workaround.md`: the OLD fix — a nightly manual dedup script. Give it an ISOLATED, distinctive proper-noun artifact that appears in NO other doc (B-01): a script named `dedup_sweeper` (or `dedup_sweeper.py`) and a component `nightly-dedup-cron`. `dedup-runbook-thread.md`: a chat/runbook thread about operating `dedup_sweeper` — reinforces the same isolated entity, still only inside workarounds_v1_8. Both tie back to the double-charge bug via the SHARED vocabulary ("double-charged") so recall finds them, but the FIX artifact names are unique to this dataset.
    Author `workarounds_v1_9/` (survives forget, supplies the flipped answer, per D-02). `release-v1.9.md`: the release note (framed exactly as `release-v1.9.md`) introducing the NEW fix — an idempotency key on the Stripe webhook handler — and stating v1.9 makes the nightly dedup approach redundant (this is the drift signal Phase 3 will use). Its fix artifact (idempotency-key webhook handler / `idempotency_guard`) must appear ONLY in workarounds_v1_9. `idempotency-fix-thread.md`: an engineering thread confirming the idempotency-key fix resolves the double-charge bug.
    Critical: the string identifying the v1.8 fix (`dedup_sweeper` / `nightly-dedup-cron`) MUST NOT appear in any incidents or workarounds_v1_9 doc; the v1.9 fix artifact MUST NOT appear in incidents or workarounds_v1_8. This isolation is what makes forget flip the answer (B-01). Keep each doc ~150-250 words (D-03 budget).
  </action>
  <acceptance_criteria>
    - Two files under `seed/workarounds_v1_8/` and two under `seed/workarounds_v1_9/`
    - `dedup_sweeper` appears ONLY within `seed/workarounds_v1_8/` (grep across the rest of `seed/` returns nothing)
    - The v1.9 idempotency artifact appears ONLY within `seed/workarounds_v1_9/`
    - `release-v1.9.md` contains "idempotency" and states the dedup approach is redundant/superseded
    - Both workaround docs reference "double-charged" so recall connects them to the incident
  </acceptance_criteria>
  <verify>
    <automated>test $(ls seed/workarounds_v1_8/*.md | wc -l) -eq 2 && test $(ls seed/workarounds_v1_9/*.md | wc -l) -eq 2 && grep -rqi 'dedup_sweeper' seed/workarounds_v1_8/ && ! grep -rqi 'dedup_sweeper' seed/incidents/ && ! grep -rqi 'dedup_sweeper' seed/workarounds_v1_9/ && grep -qi 'idempotency' seed/workarounds_v1_9/release-v1.9.md && ! grep -rqi 'idempotency' seed/workarounds_v1_8/</automated>
  </verify>
  <done>v1.8 dedup workaround and v1.9 idempotency fix authored with fully isolated fix-artifact names; the old fix lives only in workarounds_v1_8 so forget can cleanly flip recall to the v1.9 fix (B-01, DEMO-02).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author seed/README.md documenting the arc, dataset mapping, and isolation rule</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01..D-05, B-01, B-02)
    - seed/workarounds_v1_8/nightly-dedup-workaround.md, seed/workarounds_v1_9/release-v1.9.md (to describe accurately)
  </read_first>
  <files>seed/README.md</files>
  <action>
    Author `seed/README.md` documenting: the folder->dataset mapping (per D-02) `seed/incidents/` -> `incidents`, `seed/workarounds_v1_8/` -> `workarounds_v1_8`, `seed/workarounds_v1_9/` -> `workarounds_v1_9`; the before/after arc (per D-01) SEARCH "customers double-charged" -> old dedup_sweeper fix -> UPLOAD release-v1.9 -> FORGET workarounds_v1_8 -> RE-SEARCH -> new idempotency fix; the canonical demo query term (B-02) "customers double-charged"; and the isolation rule (B-01) that the v1.8 fix artifact must stay unique to workarounds_v1_8. State that the 8 docs are the human-authored source of truth and that Cognee builds the graph from them.
  </action>
  <acceptance_criteria>
    - `seed/README.md` exists and documents all three folder->dataset mappings
    - It states the canonical query term "customers double-charged" and the isolation rule
  </acceptance_criteria>
  <verify>
    <automated>test -f seed/README.md && grep -q 'workarounds_v1_8' seed/README.md && grep -q 'workarounds_v1_9' seed/README.md && grep -qi 'double-charged' seed/README.md</automated>
  </verify>
  <done>seed/README.md records the arc, the D-02 dataset mapping, the demo query term, and the B-01 isolation rule so any executor of the seed CLI understands the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored .md -> Cognee ingest | Seed docs become graph content; in Phase 1 they are fully author-controlled (trusted) |
| seed content -> git / disk | Seed docs are the committed source of truth; the cognified stores are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering (prompt/data injection via ingested docs) | seed `.md` content | low | accept | Phase 1 seed docs are author-controlled and trusted; untrusted-upload validation is a Phase-2 concern (per RESEARCH Security Domain V5). Accepted for this phase |
| T-01-02 | Information Disclosure | seeded content persisted under `.patchpilot_memory/` | low | mitigate | `.patchpilot_memory/` is gitignored (Plan 01); only the `seed/*.md` source docs are committed |
</threat_model>

<verification>
- Exactly 8 seed docs across the three dataset folders (D-03), each under ~300 words (budget).
- `dedup_sweeper` / `nightly-dedup-cron` appear ONLY in `seed/workarounds_v1_8/` (B-01 isolation).
- The v1.9 idempotency artifact appears ONLY in `seed/workarounds_v1_9/`.
- Decoys never mention "double-charged" (B-02 focus).
</verification>

<success_criteria>
- DEMO-02: bundled seed datasets tell the Stripe before/after story with isolated entity names.
- INGEST-03: the folder layout maps 1:1 to the locked dataset names, enabling surgical forget.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Record the final document count per dataset and the exact isolated entity strings chosen for v1.8 and v1.9 so the seed CLI plan can assert on them.
</output>
