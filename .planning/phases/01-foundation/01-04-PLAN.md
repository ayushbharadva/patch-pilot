---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - seed/seed_cli.py
  - scripts/snapshot_memory.py
autonomous: true
requirements: [DEMO-02, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "Running the seed CLI ingests all 8 seed docs per-dataset (add + scoped cognify) into incidents / workarounds_v1_8 / workarounds_v1_9"
    - "The CLI prints a DIFFERENT search(GRAPH_COMPLETION) answer for the same query BEFORE vs AFTER forget(workarounds_v1_8) — the before/after flip"
    - "After forget, the incidents dataset still returns results (surgical forget did not touch it), verified by inspecting Cognee storage"
  artifacts:
    - seed/seed_cli.py
    - scripts/snapshot_memory.py
  key_links:
    - "per-dataset scoped cognify(datasets=[name]) for each of the three datasets (never bare cognify) -> clean isolation"
    - "forget(dataset=workarounds_v1_8) removes only the old dedup answer; incidents + workarounds_v1_9 survive"
    - "tarball snapshot of .patchpilot_memory/ enables zero-cost re-runs (only cognify costs money)"
---

<objective>
Deliver the Phase-1 headline proof: a CLI that seeds all three datasets and demonstrates an unambiguous before/after recall flip around a surgical forget of workarounds_v1_8. This vertical slice ties the whole phase together — it consumes the locked config/naming (Plan 02) and the isolated-entity corpus (Plan 03) to produce the visible flip (ROADMAP success criterion 3).

Phase Goal (MVP framing): As the PatchPilot developer, I want one command that proves search -> forget -> re-search flips the recommended fix from the old dedup script to the new idempotency fix, so the core Cognee-memory value is proven in the CLI before any UI exists.

Purpose: Prove DEMO-02's before/after story end-to-end and verify INGEST-03 dataset isolation by inspecting storage after seeding. Add a tarball snapshot so re-runs cost $0 (only cognify bills).
Output: seed/seed_cli.py (seed + flip) and scripts/snapshot_memory.py (snapshot/restore of .patchpilot_memory/).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- **Files:** `seed/seed_cli.py`, `scripts/snapshot_memory.py`
- **CLI `seed/seed_cli.py` flags:**
  - `--seed` — add all 8 docs per dataset (`incidents`, `workarounds_v1_8`, `workarounds_v1_9`) + per-dataset scoped `cognify()`
  - `--flip` — run the before/after demo: `search(GRAPH_COMPLETION)` BEFORE -> `forget(workarounds_v1_8)` -> `search` AFTER -> print side-by-side diff + assert answers differ
  - `--reset` — restore-from-snapshot (or prune + reseed) for a clean re-run
  - default (no flag) — run `--seed` then `--flip` end-to-end
- **CLI `scripts/snapshot_memory.py` flags:** `--save` (tar `.patchpilot_memory/`), `--restore` (untar) for zero-cost reseeds
- **Canonical demo query:** "What is the fix for customers being double-charged?"
- **Dataset names consumed (from backend/datasets.py):** `incidents`, `workarounds_v1_8`, `workarounds_v1_9`
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Seed loader — per-dataset add + scoped cognify from seed/*/*.md (INGEST-02, INGEST-03)</name>
  <read_first>
    - .planning/phases/01-foundation/01-01-SUMMARY.md (confirmed add/cognify signatures + SearchType path)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (final doc list + isolated entity strings)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 per-dataset cognify; Anti-Patterns "bare cognify"; Don't Hand-Roll tarball reseed)
    - backend/cognee_config.py, backend/datasets.py (import order + name constants)
    - seed/README.md (folder->dataset mapping)
  </read_first>
  <files>seed/seed_cli.py</files>
  <action>
    Create seed/seed_cli.py. Its runtime imports MUST ensure cognee_config is imported BEFORE cognee (config-before-import keystone; insert the repo root onto sys.path first so `from backend import cognee_config, datasets` resolves when run from repo root). Import dataset name constants from backend.datasets — do NOT hardcode dataset name string literals (per D-02, INGEST-03).
    Implement `--seed`: read each seed/<folder>/*.md file, call add(<docs>, dataset_name=<constant>) for each of INCIDENTS, WORKAROUNDS_V1_8, WORKAROUNDS_V1_9, then call cognify(datasets=[<one name>]) SCOPED per dataset (three separate scoped calls — never a bare cognify(); scoping prevents the #1023 cross-dataset merge). Print progress per dataset.
    Wire an argparse interface exposing `--seed`, `--flip`, `--reset`, and a default (no flag) that runs seed-then-flip. The `--flip` body is completed in Task 2 and `--reset` delegates to the snapshot script from Task 3.
  </action>
  <acceptance_criteria>
    - seed/seed_cli.py imports cognee_config before cognee and imports dataset names from backend.datasets
    - `--seed` calls cognify(datasets=[...]) scoped per dataset (grep shows `datasets=`)
    - After running the seed, `.patchpilot_memory/databases/` is non-empty
    - argparse exposes `--seed`, `--flip`, and `--reset`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'from backend' seed/seed_cli.py && grep -q 'datasets=' seed/seed_cli.py && grep -q -- '--flip' seed/seed_cli.py && grep -q -- '--reset' seed/seed_cli.py && .venv/bin/python seed/seed_cli.py --seed && [ -n "$(ls -A .patchpilot_memory/databases 2>/dev/null)" ]</automated>
  </verify>
  <done>Seed loader ingests all 8 docs via add() + per-dataset scoped cognify() into the three locked datasets; storage fills on disk (INGEST-02, INGEST-03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Before/after flip — search -> forget(workarounds_v1_8) -> re-search with unambiguous diff (DEMO-02)</name>
  <read_first>
    - .planning/phases/01-foundation/01-01-SUMMARY.md (confirmed forget kwargs + SearchType path from spike)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (isolated entity strings for v1.8 dedup + v1.9 idempotency)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 before/after flip; Pitfall 3 forget doesn't flip)
    - .claude/CLAUDE.md (Cognee API — search/forget signatures; SearchType.GRAPH_COMPLETION)
  </read_first>
  <files>seed/seed_cli.py</files>
  <action>
    Complete the `--flip` body in seed/seed_cli.py. Define the canonical query "What is the fix for customers being double-charged?" (B-02 distinct term). Run search(query_text=Q, query_type=GRAPH_COMPLETION) to capture the BEFORE answer (expected to reference the v1.8 dedup workaround). Call forget(dataset=WORKAROUNDS_V1_8) using the exact kwarg confirmed in the Plan 01 spike SUMMARY. Run the SAME search to capture the AFTER answer (expected to reference the v1.9 idempotency fix). Assert the two answers differ and fail loudly (print FLIP FAILED, exit 1) if identical; on success print FLIP OK. Print an UNAMBIGUOUS side-by-side terminal read (labeled BEFORE / AFTER blocks; coloring optional) — output format is Claude's discretion, goal is an obvious before/after read.
    Also assert surgical isolation: after forget, run a search that targets the durable incidents bug and confirm it STILL returns results (incidents survived); print INCIDENTS SURVIVED. Optionally print the dataset list read from Cognee storage to satisfy the "verify by inspecting storage" criterion (SC4 / INGEST-03).
  </action>
  <acceptance_criteria>
    - `--flip` runs search BEFORE, forget(WORKAROUNDS_V1_8), search AFTER on the same query and asserts they differ
    - Running the flip prints FLIP OK and the BEFORE answer differs from the AFTER answer
    - Running the flip prints INCIDENTS SURVIVED (incidents dataset still returns after forget)
    - The BEFORE answer references the v1.8 dedup artifact and the AFTER answer references the v1.9 idempotency fix (spot-checked in stdout)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GRAPH_COMPLETION' seed/seed_cli.py && grep -q 'forget' seed/seed_cli.py && .venv/bin/python seed/seed_cli.py --flip | tee /tmp/pp_flip.log && grep -q 'FLIP OK' /tmp/pp_flip.log && grep -q 'INCIDENTS SURVIVED' /tmp/pp_flip.log</automated>
  </verify>
  <done>The same query returns a different GRAPH_COMPLETION answer before vs after forget(workarounds_v1_8); incidents survives — the Phase-1 before/after flip is proven in the CLI (DEMO-02, ROADMAP SC3).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Zero-cost reseed snapshot (tar save/restore of .patchpilot_memory/) + wire --reset</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Don't Hand-Roll — tar-snapshot .patchpilot_memory/ for zero-cost reruns; Pitfall 4 runaway cognify cost)
    - .claude/CLAUDE.md (budget constraint; prune API for full reset)
    - seed/seed_cli.py (the --reset flag to wire)
  </read_first>
  <files>scripts/snapshot_memory.py, seed/seed_cli.py</files>
  <action>
    Create scripts/snapshot_memory.py with argparse `--save` (tar the entire `.patchpilot_memory/` tree into a gitignored `*.snapshot.tar`) and `--restore` (extract it back, replacing the live tree) so a fully-cognified state can be restored without paying for cognify again (only cognify costs money per Pitfall 4). Import cognee_config only if it needs the MEMORY_ROOT path; the snapshot itself is pure filesystem tar work and need not import cognee.
    Wire seed/seed_cli.py `--reset` to restore from the snapshot if one exists, else prune (prune_data + prune_system) and re-seed — giving a clean, demo-ready state. Document in the module docstring that snapshotting AFTER the first successful --seed avoids re-billing cognify on every run.
  </action>
  <acceptance_criteria>
    - scripts/snapshot_memory.py exposes `--save` and `--restore`
    - `--save` produces a `.snapshot.tar` file; `--restore` recreates `.patchpilot_memory/` from it
    - `*.snapshot.tar` is covered by .gitignore (from Plan 01) — not committed
    - seed_cli.py `--reset` references the snapshot restore path
  </acceptance_criteria>
  <verify>
    <automated>grep -q -- '--save' scripts/snapshot_memory.py && grep -q -- '--restore' scripts/snapshot_memory.py && .venv/bin/python scripts/snapshot_memory.py --save && ls *.snapshot.tar >/dev/null 2>&1 && grep -q 'snapshot.tar' .gitignore</automated>
  </verify>
  <done>A tar snapshot of .patchpilot_memory/ can be saved and restored, enabling $0 reseeds; seed_cli --reset restores it (or prunes + reseeds). Runaway cognify cost (Pitfall 4) mitigated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI process -> OpenAI | Seeding runs cognify() which bills the OpenAI account |
| snapshot tar -> git/disk | The snapshot contains cognified memory (may hold ingested content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Denial of Service (self, cost) | seed cognify + repeated re-runs | medium | mitigate | Corpus is ~8 short docs on gpt-4o-mini; tar snapshot (Task 3) restores cognified state so re-runs skip cognify; $10 cap active from Plan 01 |
| T-01-02 | Information Disclosure | `.patchpilot_memory/` + `*.snapshot.tar` | medium | mitigate | Both `.patchpilot_memory/` and `*.snapshot.tar` are gitignored (Plan 01); only seed/*.md source docs are committed |
| T-01-01 | Information Disclosure | key read at cognify time | high | mitigate | seed_cli imports cognee_config which loads the key from the gitignored .env; no secret literal in seed/ or scripts/; verified by grep for `sk-` in tracked files |
</threat_model>

<verification>
- `--seed` ingests all 8 docs per-dataset with scoped cognify; storage fills on disk.
- `--flip` prints FLIP OK (BEFORE != AFTER) and INCIDENTS SURVIVED.
- Snapshot save/restore works and `*.snapshot.tar` is gitignored.
- No secret literal appears in seed/ or scripts/ tracked files.
</verification>

<success_criteria>
- DEMO-02: seed CLI produces the unambiguous before/after recall flip around forget(workarounds_v1_8).
- INGEST-02: all seed content ingested via add() + cognify().
- INGEST-03: three datasets isolated; forget is surgical (incidents survives), verified via storage inspection.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done. Record the BEFORE and AFTER answer excerpts (proving the flip), confirmation that incidents survived forget, and whether the tar-snapshot reseed path works for $0 re-runs.
</output>
