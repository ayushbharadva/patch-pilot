---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - .env.example
  - spike/spike_cognee.py
autonomous: false
requirements: [PLAT-01, PLAT-02]
user_setup:
  - service: openai
    why: "cognify() graph extraction calls the OpenAI API; without a real key Cognee's LLM connection check hangs indefinitely (GH #2119)"
    env_vars:
      - name: LLM_API_KEY
        source: "OpenAI Dashboard -> API keys (sk-... value); paste into .env as LLM_API_KEY"
    dashboard_config:
      - task: "Set a hard $10 usage cap (budget limit)"
        location: "OpenAI Dashboard -> Settings -> Limits -> Usage limits"
  - service: google-gemini
    optional: true
    why: "FALLBACK when no OpenAI key is available — Gemini free tier provides both an LLM (gemini-1.5-flash) and embeddings (text-embedding-004) at $0. Deviates from the CLAUDE.md OpenAI pin (declare in submission). Exact cognee 1.2.2 env-var names for provider/model/embedding/key MUST be confirmed via help()/docs during the spike, not trusted blind."
    env_vars:
      - name: LLM_API_KEY
        source: "Google AI Studio -> Get API key (free tier); paste into .env as LLM_API_KEY and set LLM_PROVIDER=gemini + the Gemini model/embedding vars"

must_haves:
  truths:
    - "A supported Python (3.12 preferred; 3.14.6 available locally) venv exists with cognee 1.2.2 + FastAPI 0.138.2 + uvicorn + python-dotenv + python-multipart installed"
    - "A working LLM+embedding provider is configured before any cognify runs (human-verified): OpenAI gpt-4o-mini with a $10 cap PRIMARY, or the Gemini free-tier fallback ($0, no cap needed) when no OpenAI key is available"
    - "A throwaway spike proves BOTH the forget flip and restart persistence on a 3-doc corpus against the installed cognee 1.2.2, using whichever provider is configured"
  artifacts:
    - requirements.txt
    - .gitignore
    - .env.example
    - spike/spike_cognee.py
  key_links:
    - ".gitignore lists .env BEFORE the human adds the key (secret never enters git)"
    - "root directories (SYSTEM_ROOT_DIRECTORY, DATA_ROOT_DIRECTORY) are set BEFORE import cognee inside the spike (persistence keystone, proven in miniature)"
    - "LLM_MODEL pinned to openai/gpt-4o-mini (NOT the .env.template default openai/gpt-5-mini) so cognify stays inside the $10 cap"
    - "LLM provider is switchable via LLM_PROVIDER/LLM_MODEL + embedding vars — OpenAI primary, Gemini free tier a documented $0 fallback; exact cognee 1.2.2 var names verified in the spike before relied upon"
---

<objective>
Retire the two blocking Phase-1 risks (Cognee hangs, ephemeral-FS memory loss) and the seven open research assumptions (A1-A7) in miniature, BEFORE any real code or the 8-doc seed corpus is authored. Install the verified stack on a supported Python, gate the OpenAI key + spend cap behind a blocking human checkpoint, then run a throwaway spike that `help()`s the four Cognee verbs and empirically proves the forget flip and restart persistence on a 3-doc corpus.

Phase Goal (MVP framing, backend/CLI adaptation): As the PatchPilot developer, I want the Cognee memory lifecycle (add -> cognify -> search -> forget) proven in the CLI -- running without hanging, persisting across restarts, and flipping recall after a surgical forget -- so that every UI slice in later phases builds on a de-risked, budget-safe foundation. This plan is the thinnest end-to-end slice of that lifecycle.

Purpose: Prove the approach works on THIS machine + THIS cognee version before investing in the real seed corpus and endpoints. Resolves RESEARCH.md assumptions A1-A7.
Output: A working venv, committed dependency + ignore + env template files, and a spike script whose stdout demonstrates FLIP OK and PERSIST OK.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **Files:** `requirements.txt`, `.gitignore`, `.env.example`, `spike/spike_cognee.py`
- **Env vars (declared in .env.example, human fills .env):** `LLM_API_KEY`, `LLM_PROVIDER=openai`, `LLM_MODEL=openai/gpt-4o-mini`, `SYSTEM_ROOT_DIRECTORY`, `DATA_ROOT_DIRECTORY`
- **Fallback env vars (commented in .env.example, Gemini free tier):** `LLM_PROVIDER=gemini`, `LLM_MODEL=gemini/gemini-1.5-flash`, `EMBEDDING_PROVIDER=gemini`, `EMBEDDING_MODEL=gemini/text-embedding-004` (exact key names confirmed in the spike; commented out by default so OpenAI stays primary)
- **Runtime:** `.venv/` (gitignored) on Python 3.12 (preferred) or 3.14.6 (local fallback)
- **Spike-only dataset names (throwaway):** `spike_incident`, `spike_workaround` (deleted with prune at end of spike; NOT the real dataset names)
- **Verified facts recorded in SUMMARY:** exact `cognee.forget(...)` kwargs, `SearchType` import path, whether `ENABLE_BACKEND_ACCESS_CONTROL` is default-on in 1.2.2 (resolves A1-A7)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create dependency, ignore, and env-template files + install stack in a venv</name>
  <read_first>
    - .claude/CLAUDE.md (Recommended Stack table + Installation section + "What NOT to Use")
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table, Package Legitimacy Audit, Environment Availability)
  </read_first>
  <files>requirements.txt, .gitignore, .env.example</files>
  <action>
    Author `requirements.txt` pinning exactly: `cognee==1.2.2`, `fastapi==0.138.2`, `uvicorn[standard]>=0.49`, `python-dotenv>=1.2`, `python-multipart>=0.0.32`. These five are the RESEARCH.md Package Legitimacy Audit set (all dispositioned Approved; SUS verdicts are false positives from missing download telemetry, zero SLOP).
    Author `.gitignore` FIRST and ensure it lists `.env`, `.patchpilot_memory/`, `.venv/`, `__pycache__/`, `*.pyc`, and `*.snapshot.tar` — `.env` MUST be ignored before Task 2's checkpoint puts a real key in it.
    Author `.env.example` (committed template, NO secret value) declaring the full env surface: `LLM_API_KEY=` (empty placeholder), `LLM_PROVIDER=openai`, `LLM_MODEL=openai/gpt-4o-mini` (explicitly pinned — do NOT rely on the cognee .env.template default which is now `openai/gpt-5-mini` and would blow the $10 cap), `SYSTEM_ROOT_DIRECTORY=` and `DATA_ROOT_DIRECTORY=` (documented as absolute paths under `.patchpilot_memory/`).
    In `.env.example`, ALSO add a clearly-labelled COMMENTED "Fallback: Google Gemini free tier (no OpenAI key needed, $0)" block with the alternative vars commented out: `# LLM_PROVIDER=gemini`, `# LLM_MODEL=gemini/gemini-1.5-flash`, `# EMBEDDING_PROVIDER=gemini`, `# EMBEDDING_MODEL=gemini/text-embedding-004`, plus a comment noting the same `LLM_API_KEY=` slot holds the Gemini key on that path and that the exact cognee 1.2.2 var names are verified by Task 3's spike. OpenAI stays the active/uncommented provider.
    Create a venv on a supported Python (prefer `python3.12`; fall back to the locally-available `/opt/homebrew/bin/python3.14`, treating any install friction as a spike finding per A7) and `pip install -r requirements.txt` inside it. Use `pip` inside the venv (uv is not installed on this machine).
  </action>
  <acceptance_criteria>
    - `.gitignore` contains lines `.env`, `.patchpilot_memory/`, and `.venv/`
    - `.env.example` contains `LLM_MODEL=openai/gpt-4o-mini` and both `SYSTEM_ROOT_DIRECTORY` and `DATA_ROOT_DIRECTORY` keys, and its `LLM_API_KEY` value is empty
    - `.env.example` contains a commented Gemini fallback block including `# LLM_PROVIDER=gemini` and `# EMBEDDING_MODEL=gemini/text-embedding-004` (commented, so OpenAI remains the active provider)
    - `requirements.txt` contains `cognee==1.2.2` and `fastapi==0.138.2`
    - `.venv/bin/python -c "import cognee, fastapi; print(cognee.__version__)"` prints `1.2.2` (or the installed version) without error
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^\.env$' .gitignore && grep -q 'LLM_MODEL=openai/gpt-4o-mini' .env.example && grep -q 'cognee==1.2.2' requirements.txt && .venv/bin/python -c "import cognee, fastapi"</automated>
  </verify>
  <done>venv installs the five pinned packages cleanly; .gitignore ignores .env/.patchpilot_memory/.venv; .env.example declares the full env surface with gpt-4o-mini pinned and no secret value.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human checkpoint — add OpenAI key to .env and set the $10 spend cap</name>
  <action>BLOCKING HUMAN CHECKPOINT (not auto-executable). The human configures ONE provider in .env — OpenAI (primary) OR Gemini free tier (fallback if no OpenAI key). Do not proceed to the spike until confirmed — cognify() hangs without a working provider key (GH #2119), and the OpenAI path risks budget without the cap.
  PRIMARY (OpenAI): copy .env.example to .env, paste the OpenAI sk- key into LLM_API_KEY, fill both absolute root-dir paths, set a hard $10 usage cap in the OpenAI dashboard, confirm LLM_MODEL=openai/gpt-4o-mini.
  FALLBACK (Gemini free tier, $0): copy .env.example to .env, paste a Google AI Studio free-tier key into LLM_API_KEY, fill both absolute root-dir paths, and uncomment the Gemini block (LLM_PROVIDER=gemini, LLM_MODEL=gemini/gemini-1.5-flash, EMBEDDING_PROVIDER=gemini, EMBEDDING_MODEL=gemini/text-embedding-004) while commenting out the OpenAI lines. No cap needed (free tier). Note the provider deviation in the submission.</action>
  <what-built>requirements.txt / .gitignore / .env.example are committed and the venv is installed. `.env` is gitignored so a real key can be added safely.</what-built>
  <how-to-verify>
    1. Copy `.env.example` to `.env` (the copy is gitignored — confirm with `git status` that `.env` is NOT listed as untracked/tracked).
    2. Paste your provider key as the value of `LLM_API_KEY` in `.env` — an OpenAI `sk-...` key (primary) OR a Google AI Studio free-tier key (fallback).
    3. Fill `SYSTEM_ROOT_DIRECTORY` and `DATA_ROOT_DIRECTORY` with absolute paths under this repo's `.patchpilot_memory/` (e.g. `/Users/.../patch-pilot/.patchpilot_memory` and `/Users/.../patch-pilot/.patchpilot_memory/data`).
    4a. OpenAI path: in the OpenAI Dashboard -> Settings -> Limits, set a hard **$10** usage cap; confirm the model line reads `LLM_MODEL=openai/gpt-4o-mini` (NOT gpt-5-mini).
    4b. Gemini path: uncomment the Gemini fallback block (provider/model/embedding vars) and comment out the OpenAI lines; no spend cap needed on the free tier.
    5. Exactly ONE provider should be active (uncommented) in `.env`.
  </how-to-verify>
  <resume-signal>Type "approved" once ONE provider is configured in .env (OpenAI + $10 cap, or Gemini free tier), and `git status` shows .env is ignored. Or describe any blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Throwaway Wave-0 spike proving the forget flip and restart persistence (resolves A1-A7)</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Assumptions Log A1-A7, Pattern 1 config-before-import, Pattern 2 per-dataset cognify, Pattern 3 before/after flip, Pitfalls 1-3, Code Examples)
    - .claude/CLAUDE.md (Cognee API Reference — exact signatures for add/cognify/search/forget/prune; SearchType enum)
  </read_first>
  <files>spike/spike_cognee.py</files>
  <action>
    Write a SELF-CONTAINED throwaway script (it does NOT import project code — cognee_config does not exist yet). At the very top, before `import cognee`, call `load_dotenv()` so whatever provider `.env` configures wins, THEN `os.environ.setdefault(...)` for `SYSTEM_ROOT_DIRECTORY`, `DATA_ROOT_DIRECTORY`, and — only as a last-resort default when `.env` set nothing — `LLM_MODEL`/`LLM_PROVIDER` (Pattern 1 keystone — proves persistence config in miniature). Use `setdefault` (never hard assignment) so the Gemini fallback path (`LLM_PROVIDER=gemini` + embedding vars in `.env`) is honored without editing the spike.
    Print the resolved provider/model (`LLM_PROVIDER`, `LLM_MODEL`, and `EMBEDDING_*` if set) at startup so the SUMMARY records which path (OpenAI primary or Gemini fallback) the run actually exercised.
    First, print the runtime contract to resolve assumptions: `help(cognee.forget)`, `help(cognee.add)`, `help(cognee.cognify)`, `help(cognee.search)`, and the resolved `SearchType` import path — record the exact `forget` kwarg name (A4) and import path (A5) in stdout so they land in the SUMMARY. If the Gemini path is used, ALSO record the exact cognee 1.2.2 env-var names that actually drove provider/embedding selection (confirms the fallback var names for downstream plans).
    Then run the lifecycle on a MINIMAL 3-doc corpus: `add()` one durable-incident sentence to dataset `spike_incident`; `add()` one workaround sentence naming a UNIQUE artifact (an isolated entity that appears in no other doc, e.g. a made-up script/component proper noun) to dataset `spike_workaround`; per-dataset scoped `cognify(datasets=[...])` for each (NEVER a bare cognify — Pattern 2 / Pitfall 3).
    Prove the FLIP: run `search(query_type=GRAPH_COMPLETION)` on a query that hits the workaround BEFORE, then `forget(dataset="spike_workaround")`, then the SAME search AFTER; assert the two answers differ and print `FLIP OK` (else `FLIP FAILED`). This empirically resolves A1/A2/A3.
    Prove PERSISTENCE: after the flip, re-open/read the graph state (fresh cognee search in the same process is insufficient — the script must exit and be re-invoked, OR spawn a subprocess with the same root dirs that searches `spike_incident` WITHOUT re-adding/re-cognifying and asserts the incident survives); print `PERSIST OK`. This resolves the PLAT-02 mechanism in miniature and confirms Pitfall 2 is avoided.
    End by calling prune (`prune_data` + `prune_system`) to leave `.patchpilot_memory/` clean for the real seed. This spike is throwaway (may be deleted after Phase 1) but is committed for traceability.
  </action>
  <acceptance_criteria>
    - `spike/spike_cognee.py` sets `SYSTEM_ROOT_DIRECTORY`/`DATA_ROOT_DIRECTORY`/`LLM_MODEL` before the cognee import (config-before-import)
    - Running the spike prints the resolved `forget` signature and `SearchType` import path
    - Spike stdout contains `FLIP OK` (BEFORE and AFTER GRAPH_COMPLETION answers differ after forget)
    - Spike stdout contains `PERSIST OK` (the durable incident survives a fresh-process read without re-cognify)
    - After the spike, `.patchpilot_memory/` is pruned clean (no leftover spike datasets)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python spike/spike_cognee.py | tee /tmp/pp_spike.log && grep -q 'FLIP OK' /tmp/pp_spike.log && grep -q 'PERSIST OK' /tmp/pp_spike.log</automated>
  </verify>
  <done>Spike runs against installed cognee 1.2.2 without hanging, prints the four verbs' signatures, and demonstrates both FLIP OK and PERSIST OK. Findings (exact forget kwargs, SearchType path, access-control default) recorded in SUMMARY, resolving A1-A7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local files -> git (VCS) | Secrets at rest (`.env`) must never cross into version control |
| local process -> LLM provider API (OpenAI or Gemini) | The `LLM_API_KEY` secret crosses to a third party; on the OpenAI path spend is unbounded without a cap (Gemini free-tier path is $0) |
| PyPI -> local venv | Installed packages execute code; supply-chain tampering risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` / `LLM_API_KEY` (OpenAI or Gemini key) | high | mitigate | `.gitignore` lists `.env` in Task 1 BEFORE the Task 2 checkpoint adds the key; only `.env.example` (empty value + commented fallback block) is committed; SUMMARY verifies no `sk-`/`AIza`-style key string in tracked files |
| T-01-03 | Denial of Service (self, cost) | cognify spend | medium | mitigate | Hard $10 OpenAI dashboard cap (Task 2 checkpoint); `LLM_MODEL` pinned to `openai/gpt-4o-mini`; spike corpus is 3 tiny docs; prune at end so no accidental re-cognify. Gemini fallback path is free-tier ($0), removing the spend surface entirely on that path |
| T-01-SC | Tampering (supply chain) | pip installs (5 pkgs) | high | mitigate | RESEARCH.md Package Legitimacy Audit verified all five (cognee, fastapi, uvicorn, python-dotenv, python-multipart) as long-lived projects with real repos; dispositioned Approved; zero SLOP. SUS verdicts are false positives from missing PyPI download telemetry, so no separate blocking package checkpoint is warranted; versions are pinned in requirements.txt |
</threat_model>

<verification>
- Stack installs on a supported Python (3.12 preferred, 3.14.6 fallback) with no build failure.
- `git status` confirms `.env` is ignored; no `sk-` secret appears in any tracked file.
- Spike demonstrates FLIP OK + PERSIST OK against installed cognee 1.2.2 without hanging.
- Assumptions A1-A7 are each marked resolved/updated in the SUMMARY based on spike output.
</verification>

<success_criteria>
- Venv with the five pinned packages installed and importable.
- Blocking human checkpoint passed: key in `.env`, $10 cap active, model pinned.
- `spike/spike_cognee.py` prints FLIP OK and PERSIST OK; the forget flip and restart persistence are proven in miniature before the real seed corpus is authored.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Record: chosen Python version, exact `cognee.forget()` kwargs, `SearchType` import path, whether `ENABLE_BACKEND_ACCESS_CONTROL` is default-on in 1.2.2, and the resolution status of each assumption A1-A7 — downstream plans depend on these facts.
</output>
